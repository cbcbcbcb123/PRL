---
document_id: PRL-MIXED-CUBE-REFINEMENT-V01
date: 2026-09-19
authorization: user_authorized_next_L2_refinement_milestone_20260919
status: not_run
---

# 下一个完整问题：原制造解加密后能否补上L2缺阶

用户在上一里程碑报告“只针对剩余收敛阶做加密验证”之后明确决定：
“好的，继续，完成下一个大问题”。沿用连续处理一个问题、不逐微步审批的授权，
本次仅登记下面一个新的求解；不是恢复旧批次或无限扫描。

## 事先固定的对照与裁决

- 复用已冻结P3/P2的n4、n8原始状态；新增且仅新增n12的原MMS，一次SNES、零初值。
- μ=1、κ=100、u*=(0.002sinπXsinπYsinπZ,0,0)，p*=κ(J*−1)。
  同一单位立方体、六Freudenthal四面体/体素、P3/P2、生产Q8、原有限变形NH、
  X=0解析位移及其余五面解析Piola牵引与配套体力；不改变本构、载荷、边界或求解容差。
- 新序列固定为4/8/12；唯一主要阶次是8→12，使用ln(E8/E12)/ln(12/8)，不能继续除以ln2。
  完整披露4→8历史阶次；不在结果出来后换网格对、剔除分量或改范数。
- 三项误差均须逐级下降，主要阶次仍u L2≥3.5、u梯度H1半范数≥2.5、p L2≥2.5。
  n12绝对门仍u L2≤2%、u H1≤15%、p L2≤15%、J误差RMS≤0.1%。
  独立方程、映射、正J及所有保存路径必须通过。6/8点每轴独立误差积分均披露。
- 原2/4/8的u L2阶3.37274及原心室1%门的failed永远保留；新序列通过也不追认旧门。
  缺失任何必需状态为blocked；接口/安全或实际误差门失败为failed。未运行不补造。

## 资源决定在新结果前完成

n8原生混合DOF=51,788，求解46.843秒、进程累计RSS高水位1.301GiB；并非隔离单例峰值。
n12=167,584 DOF、10,368四面体。n4/n8两点外推的RSS情景约3.98–5.58GiB；
按DOF线性外推约4.21GiB。均不是硬上界；容器仍强制8GiB、单CPU、1800秒含120秒保全。
求解、写入、内存或时间失败时保存已有状态并停止，不自动重跑。

在任何新结果之前明确不运行n16：388,884 DOF，仅按DOF线性外推已约9.77GiB，
且稀疏LU因子可能超线性增长。曾讨论的8/12/16方案未冻结、未执行，因资源风险改为4/8/12，
不是看到新误差后改主要网格对。没有把“额度不限”解释为放开运行资源或GPU。

## 执行与保全

create-only：`E:\Temp-Projects\PRL-results\ventricle_fem\mixed_cube_refinement_v01_20260919`。
固定当前本地镜像及现有运行配方；最多1容器、1求解，0GPU、0自动重试。
磁盘至少10GiB余量、估计新增512MiB及停止保全128MiB，仓库3GiB上限保持。
不启动/修复Docker、不拉取/安装/更新、不删除/改权限/创建映射，不运行心室、主动、生长、FSI或DCM。

保护父包：representation_v02最终manifest
`152416a7709b0f485b97e94a53ff4598f4b00b4d66339b7f84bdd9c41abb2a5f`；
representation_v03最终manifest
`61550554e2534bcfaadfe8ac9ec3e3218e33275a5a81d5ab110758b53c28de28`。
原八工况合同SHA-256=`9a13d99182bb003bf0f944d4b2df92cb3530f30cc3bfb17533b074fba4a3fb0d`。
历史失败、专家原件、50项无关改动和执行源码快照前后核验；大数组留批准结果库。

首轮只读Docker doctor的引擎API正常，但镜像查询15秒超时；WSL当时Stopped。
新backend日志13:20:04 UTC已明确记录自动idle唤醒完成与VM started，这提供了状态变化证据，
允许随后按当前运行器进行一次有界镜像/API检查，不是重复启动Desktop或修复。
活跃socket的ACL1920仍照录，不单独判损坏。任何后续API/镜像不合格即不启动科学容器。

交付一个真实结构/误差/算法状态诊断页，沿用160dpi探索例外、可执行Notebook与源码快照。
完成后更新导航、独立复核、直接提交main并普通推送origin/main，不另建评审包。
