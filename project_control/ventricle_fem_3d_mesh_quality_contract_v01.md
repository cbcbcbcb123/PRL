---
document_id: PRL-F6-S2-D2A-MESH-QUALITY-V01
status: adopted
date: 2026-09-18
authority: user 同意，继续 after the mesh-quality audit and controlled unstructured comparison proposal
---

# F6-S2-D2A：先审查网格质量与已有体积误差的关系

本切片落实用户批准建议的第一步：保留现有基准，独立审查M0/M1参考网格，
与实存p/mu=0.01、Ta=0状态逐单元对齐。不是新的FEM加载或物理修复。
冻结父包f6s2d1_3d_fine_pressure_v01_20260918的manifest SHA-256：
7bd712585b82c5ca4689f56292536a918b060ace2acf9051790ecba9a790e0f4。

## 当前范围

- 只从已保留网格、完整u/p及配置重算几何质量与J；零次FEM求解。
- 度量：最小/最大内部二面角、最长/最短边比、最长边/最小高、
  正则四面体归一化映射的2范数条件数、mean-ratio、3r/R半径比。
- 按层、是否接触固定基底、是否超过原max abs(J-1)=1%门分组，
  记录全体分布和描述性Spearman相关，不把单元当作独立生物样本，不做显著性推断。
- q_radius<0.2或最小二面角<10度仅为预先声明的筛查标记，不是通用合格门；
  不改已有失败裁决，不以相关性证明因果。
- 解析正四面体、平移/旋转/缩放/顶点重编号不变性、退化输入等回归测试；
  原三维生产力学、几何生成器和独立力学验证器保持原哈希。
- 可执行一次只检查既有固定本地FEniCSx镜像内网格工具的短探针；
  1CPU、禁网、只读根、0GPU、不安装/拉取/重启、无删除，最多60秒。
  不因探针通过自动启动FEM。若缺少工具，报告工具前提，不自行安装。

## 后续对照的准备

以审查结果形成非结构化网格对照合同，明确保持几何/分层/材料/P2-P1/压力/
基底条件，区分边界几何变化、局部加密、拓扑变化。先查工具和网格可行性，
不能将“更杂乱”作为质量改善。若需要安装、扩大自由度/预算或改变边界等，另行确认。
本诊断只交付已完成的离线证据与准备状态；新候选及新加载用单独冻结合同实施。

## 交付与保护

create-only结果：E:\Temp-Projects\PRL-results\ventricle_fem\f6s2d2a_mesh_quality_v01_20260918。
预计新增128MiB，64MiB停止空间、10GiB磁盘余量，代码仓3GiB门保持。
保存逐单元数值、完整必要输入副本、来源SHA、可执行Notebook、结构/质量/误差图、
600dpi PNG、SVG和诊断结论；此离线阶段不是时间轨迹，不补造新平衡态。
保全全部父/祖先证据和50项无关修改。任务临时目录tmp/f6s2d2a_checks，
所有临时产物列明用途；未经新的精确清单确认不删除。
同步导航与严格链接核验，按既有规则本地main精确提交，不推送。
