---
document_id: PRL-F6-S2-R-IDEALIZED-3D-RESUME-V01
status: adopted
date: 2026-09-18
authority: user 确认执行 following the explicit remaining-13-state proposal
parent_contract: project_control/ventricle_fem_idealized_3d_contract_v01.md
---

# F6-S2-R：复用原无载，续原定13态

本合同只恢复上轮未运行部分，不重新求解M0无载，不扩大科学问题。
父包f6s2_idealized_3d_v01_20260918的manifest SHA-256：
991710ecfb62d72bca971f8254199f119518d8fe199312e1bcea7949645d3e42。
该包的原运行/原生复核failed保持；修正读取后的无载离线资格是独立记录。

## 固定科学规格

原半椭球几何、三层、基底固定/外壁自由、P2/P1、mu=1、kappa=1000、
切平面等权分散主动能量、随动腔压、六阶积分及所有原门限不变。
输入几何逐字节复制，不重新生成。生产类的构造/solve/monitor和独立力学函数
必须与父包sources_at_delivery逐函数AST一致，仅增加续算、身份检查和交付编排。
旧二维及此前失败证据不改写。新结果create-only：
E:\Temp-Projects\PRL-results\ventricle_fem\f6s2r_idealized_3d_resume_v01_20260918。

## 顺序与停止

一次固定本地镜像容器，先重新建立同一M0函数空间，但不调用无载SNES：
核对全部网格、层、边界、积分点/权重及DOF坐标；两张混合映射只允许(N,)与(1,N)
布局规范化后数值完全相同。不得插值或重排成“近似相同”的初态。
读取原混合向量，检查生产端当前压力输出确为(N,)、u为(Nu,3)，
重新装配残差/求值F/J/应力并与保留数据比较，独立审计仍用原门；向量前后不变。
这个接口门没有求解或Newton更新；通过后才允许首个新压力态。

M0六态：p=0.01/0.02且Ta=0；p=0且Ta=0.05/0.10；p=0.02且Ta=0.05/0.10。
三个分支初值分别为M0原zero、原zero、接受的pressure_2，分支内按原规则延拓。
M0通过后才M1原七态（包含其首次无载）；至多13次新SNES调用。
首个接口/求解/独立状态门失败即停，保留首次异常及最后有效状态，不调参或自动新建重跑。
最终粗细响应门按原0.002绝对且5%相对；不能把均值或无载J=1代替原1%局部门。

原1800秒外部时限、内部预留120秒停止保全、1CPU/8GiB/禁网/0GPU保持。
运行前只读检查Docker及镜像；不启动/重启/修复、不安装/拉取、不另起接口微测容器。
存储预测768MiB、停止保全至少64MiB、磁盘余量10GiB，代码仓仍3GiB。
只允许本项目与已批准结果库写入；无删除、目录迁移、Git推送。

## 核验与图件

13态是上限不是完成承诺。每个新态保存初态、全混合向量、每次Newton状态、残差、
压力、F/J/总与主动应力以及SNES/MUMPS读回；原无载拷贝和本轮状态标明来源。
生产运行和独立NumPy验证不共享力学公式；协议函数仅处理路径/调用次序/身份。
本机与容器独立报告分类必须一致，普通数值字段比较rtol1e-10/atol1e-12；
仅两个中心有限差分虚功误差允许1e-9跨平台舍入差，且两端原2e-6物理门均须通过。
该比较门在计算前冻结，不修改物理门限。

至少结构图和裁决结果图；若至少5个状态可用，展示原zero、pressure_1/2、combined_1/2
五个真实状态，另示主动单独对照。早停时只展示实存态，missing明确not_run。
显示实际1倍形变、应力、局部体积误差；加载级不是生理时间。
Notebook、600dpi PNG和SVG可复算；保留父包/祖先及50项既有脏文件哈希。
本轮无生长、FSI、实验标定或生物学吻合声明；完成交付后精确暂存并本地main提交。
