---
document_id: PRL-F6-S2-D1-3D-FINE-PRESSURE-V01
status: adopted
date: 2026-09-18
authority: user 同意，继续 after the M1 zero plus same-pressure proposal
---

# F6-S2-D1：已有细网格的两态诊断

仅检验已生成M1离散是否降低基底附近体积失真。不是补完原12态，更不是生长/FSI授权。
父包f6s2r_idealized_3d_resume_v01_20260918的manifest SHA-256：
cb846ad96711d6706a7a9b2912ddf9100a65c2a3645e9dd7f7af14c0eafd455c。
原M0首压力failed及其原始向量、场量、日志全部保留，不重新求解M0。

## 固定模型、范围与门限

原M1几何文件逐字节复制：3960四面体，周向24、经向6、径向[1,1,3]。
同半椭球、三层、P2位移/P1连续压力、六阶积分、mu=1/kappa=1000；
基底环全位移固定，外壁自由，内腔随动压力。Ta=0，无主动分支。
使用原生产、协议和独立力学验证代码，文件哈希必须与父包交付快照一致；
仅运行编排/配置选择/离线对照及绘图新增，不改方程、求解器、材料或局部门。

M1最多两次新SNES：zero(p=Ta=0)，然后pressure_1(p=0.01,Ta=0)，初值为接受的M1 zero。
首个状态/接口失败即停，不插值粗网格初值、不自动重跑、不尝试p=0.02或收缩。
保持原合同全部逐状态门，特别是积分点+每单元额外56点max abs(J-1)<=1%。
不是只看均值，不以收敛残差或图件通过代替此门。

离线从原M0和新M1的完整u/p数据独立重算并比较：
腔体积变化、最大位移、J最大偏差、超限单元数、基底相邻/远区及层分布。
两网格体积响应差仍采用0.002绝对且5%相对作为诊断参考门；
即使该门passed，也不能改写M0局部failed，不能宣称热点或渐近收敛。
新切片status由M1全部原门及该响应对照决定；完整3D加载资格仍未取得。
两网格并非同边界几何的纯h细化（曲面近似也改变），因此不能单独分离几何/网格/夹持/压力空间贡献。

## 运行与证据

新create-only结果：E:\Temp-Projects\PRL-results\ventricle_fem\f6s2d1_3d_fine_pressure_v01_20260918。
一次固定本地FEniCSx镜像容器，1CPU/8GiB/禁网/0GPU，1800秒外限/内部120秒保全预留。
Docker只读检查，不启动/重启/修复/安装/拉取。无删除、迁移、推送或外部新路径。
新增预计768MiB，至少64MiB停止空间和10GiB磁盘余量，代码仓3GiB门保持。
每次Newton状态、初态、全混合向量、u/p/F/J/应力、残差和MUMPS读回完整保存。
保全父/祖先1940文件、50项无关修改；最终以实际核验数为准。
主机与容器原始verify报告分类一致，普通数字rtol1e-10/atol1e-12；
仅两个有限差分虚功误差字段沿用1e-9舍入差门，各自物理门仍是原2e-6。

至少交付结构与裁决图；从真实无载/首压力画粗细网格，应力及J共同色标、1倍形变。
只有实际两态，不补造五个平衡态；Newton进度不是生理时间。
Notebook及物理数据副本、600dpi PNG/SVG、失败/未运行项均明确保存。
通过验收后按既有规则本地main精确提交，不推送。

本轮结束后再次停在裁决点，不因诊断通过自动扩展其他加载/边界/离散。
