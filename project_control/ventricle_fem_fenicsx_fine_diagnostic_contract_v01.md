---
document_id: PRL-FEM-FENICSX-FINE-DIAGNOSTIC-V01
status: adopted
authorization: user said continue after the explicit two-state fine-grid proposal
result_root: E:/Temp-Projects/PRL-results/ventricle_fem/f6s1q_fine_diagnostic_v01_20260917
---

# F6-S1-Q：仅补细网格两个状态

用户在结果库验收及“只补细网格零载和首个压力态”的建议后明确“继续”。
本合同冻结该有界切片，不重跑M0、不改变材料/载荷/步长/门限、不加入主动收缩。

复用candidate_0原哈希及中点四分，与F6-S1-P已保存M1_input_mesh逐数组相等后才求解。
M1为14164单元、7457顶点。M0仅复制旧状态供独立对照，0次新M0求解；0次Gmsh。
新平衡最多2次：p/mu=0和0.02。首个失败即停止，不自动重试或转入后续压力态。

保持P2位移/P1连续压力、六阶积分、三维平面应变NH、mu=1/kappa=1000；
同一实测外轮廓、构造内腔/三层、A固定ux/uy、B固定uy、内壁随动压力、外壁自由。
所有逐态门沿用，包括局部max|J−1|<=1%、原非线性残量、独立F/J/应力/力/虚功门。
对照报告原腔面积差门（绝对<=0.002且相对<=0.05）、J峰值/加权均值/超限参考体积分数、
各层超限单元和热点位置。两档结果最多支持敏感性判断，不宣称渐近收敛、实验拟合或完整被动资格。

一次容器，最多1200秒，1 CPU、8 GiB、0 GPU；只用已固定本地镜像、禁网、不安装/拉取/重启。
新结果按已批准外部结果库与磁盘余量门保存。预计256 MiB（不是容量上限），停止空间至少64 MiB、
磁盘安全底线10 GiB。全部父证据保持，结果create-only；失败保存首次异常、最后有效态及原始场量。

独立复算不调用UFL生产力学；图件从实际保存状态生成：结构、同尺度形变/应力、局部J粗细比较，
不足五态就按实际两态展示；加载级不是生理时间，不生成假心动周期。
结构图、结果图、Notebook、PNG/SVG和明确标注两态的预览保留于新结果包。
交付验收后小型索引/代码/合同直接提交本地main，不推送；不删除或迁移任何旧证据。
