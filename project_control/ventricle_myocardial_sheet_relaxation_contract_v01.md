---
status: frozen
execution_status: authorized
date: 2026-09-14
stage: Z1-MYO-SHEET-M0-RELAX-V01
---

# 心肌层静态维持：前置检查与条件执行合同

用户在接触修复通过报告后回复“同意，继续”，授权项目内CPU前置检查及通过后的16胞静态松弛。依赖C0修订v02 passed和G0几何passed；不重跑/覆盖旧正式包、不改变原阈值。无GPU、删除、外部工作区写入、安装、ECM、心内膜或周期驱动。

## Q：接触求积独立细化

保持384面闭合双胞几何不变，END/SIDE、间隙-.035/+.070，改变接触求积最大边长.20/.10/.05，共12次静态调用。对比.10到.05：法向合力及能量相对差<=2%，节点接触牵引面积加权L2差<=5%；各分母使用细档非零范数，零值按绝对1e-12判断。原ka=.04、kr=.16、cutoff=.35不变。必须有限、方向正确、合力合矩<=1e-10。Q失败则D与16胞松弛not_run；不能以细化自由度替代求积细化。

## D：短程双胞完整力学

Q通过后选择.10求积（必须已对.05通过）。使用END/SIDE初始+.070两种相邻方式，两个细胞全部自由，保留完整皮质/体积/面积内力及恒定骨架；没有永久跨隙链接。算法松弛坐标终点1.0，dt=.02/.01，共4条轨迹，保存0/.25/.5/.75/1五个实际状态；不是生理时间。对比细档末态：节点位移L2变化<=2%（分母细档总位移，地板1e-12）；体积误差<=2%、最小角>=15°、接触支持非零、积分力做功/阻尼代数残差<=1e-10。D只检验短程稳定性和步长敏感性，不要求此时达到1e-3平衡残力。D失败不启动整片。

## M：16胞整片静态松弛（只有Q和D通过才执行）

复用G0保存的4×4隔行错列六棱柱384面每胞；16个类型4心肌细胞，所有不同细胞间采用已经合格的显式表面接触。无外部压缩框、ECM或腔面压力。长轴沿x。各行最左胞外侧x端帽、最右胞外侧x端帽上的节点固定xyz；以各胞初始局部x极值面且外侧列身份选帽，容差1e-8。不固定整颗边缘细胞，不对内界面施夹持。上下和横向表面自由。

材料沿用既有心肌/条带而不是C0清空内力探针的初始化占位：皮质张力.160、体积模量30、总面积模量.050，弯曲0；各胞初始真实体积为固定目标体积，面积等周参数按初始几何初始化，不含目标形状或参考边长能。接触ka=.04、kr=.16、cutoff=.35。骨架沿用原方向性预应力幅值.060、diag(1,-.15,-.85)，面/节点法向投影和旧条带的去刚体分量策略；它是非保守胞内主动预应力而非参考态膜能，累计做功单独记录。周期收缩模块关闭。阻尼使用原核心节点对偶面积×面阻尼密度1，相对静止背景，注明合成松弛尺度。

显式过阻尼核心advance_surface_overdamped；所有细胞的力在同一旧状态组装后同步更新，不在逐胞更新中重新计算邻居力。dt=.01，最大4000步（算法坐标40），保存0/1000/2000/3000/4000步，以及异常状态。单步位移不超过当前最短边8%；越过预算或步长安全条件即停止，不自动延长或调材料。通过条件：所有状态有限、体积误差<=2%、保存最小角>=15°、固定节点位移<=1e-10；末态最大自由节点力<=1e-3；33个原相邻界面仍具正支持面积且接触图连通；无未说明的表面穿透/自交。体积5%或角8°触发即时安全停止；所有失败状态保留。若出现穿透，不能用捕获距离内的正面积判作几何通过。

输出逐胞尺寸/体积、状态压力/曲率、接触/骨架/反力分量、自由残力、面积/邻接与间隙诊断；原始拓扑与节点、步审计、命令及运行前源/输入哈希。初始/中途状态不冒充平衡末态；组织即使不稳定也出真实结构及失败图。

## 预算与停止

单线程CPU：Q总<=120s，每例<=30s；D总<=600s，每例<=150s；M最多1800s，单步最多30s；编译另计<=600s。不得改变预算或使用GPU兜底。输出<=2GiB；内存保护2GiB，运行时检查工作集。Q/D失败停止后续，不用自动重跑找通过。M完成时间未到或残力不达标明确failed/blocked原因，不能仅因有16胞图就宣称组织稳定。

create-only结果根：results/ventricle_z1/z1_myo_sheet_relaxation_v01_20260914。Q/D/M分别子目录；只有实际运行才生成结果。绘图遵循已确认定量10×5in轴框，空间图保持真比例，GIF明确算法进度。
