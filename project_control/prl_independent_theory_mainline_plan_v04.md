---
plan_id: PLAN-PAPER2-SCIENCE-FIRST-V04
status: approved_for_bounded_execution
approved_by: human_user
approved_at: 2026-09-05
approval_source: explicit_implement_this_plan_in_supervisor_task
supervisor_thread_id: 01a067cf-d740-7d50-9ada-4743e9f19141
executor_thread_id: 019fc73d-393d-71a3-98cb-d3c0cd0c8eda
supersedes_mainline: project_control/prl_independent_theory_mainline_plan_v03.md
standing_authority: project_control/paper2_autonomous_execution_and_chart_reporting_decision_v01.md
evidence_level: exploratory_scientific_feasibility
---

# Paper 2 v04：先判断科学主线，再完善模型与工程

## 1. 目标、授权与科学边界

人类已明确要求实施本计划。总管可下发、验收并衔接本轮边界明确的工作，不再逐工况请求批准。
本计划取代旧主线中“完整 Figure 2 认证后才可探索”的执行顺序，不降低或修改正式 Figure 2 的验收标准。
旧合同、失败包、已封存证据保持原样；旧 PRECHECK-only、C1-only 禁令不再约束本轮独立探索。

中心问题：心肌驱动经过有限厚度的黏弹 ECM 后，整体收缩与心内膜局部机械负荷何时一致、何时分离，能否用简洁理论预测？
目标期刊固定为 Nature Physics，不自动以降档为出口；目标不等于成熟度或录用判断。
阴性结果、反例或无新意结论均为有效交付，用来修订科学假设而不是包装阳性。

- 心肌只有主动 FEM，目前为规定主动应变，不是完整主动张力调控；不恢复心肌 DCM 或 comparator。
- ECM 为黏弹 FEM，心内膜为离散弹性链；不声称真实细胞形变、重排、纤维化或疾病发生已可预测。
- 当前仅二维理想体、线性三层力学；器官方向为心室/EFE，旧主线的整心房表述不再有效。
- 不加入流体、真实三维几何、新生物反馈或非线性；不从线性响应声称分岔、不可逆重塑或相变。
- 先排除经典黏弹滤波、平均与非线性观测量运算造成的平凡差异；粗粒化和传递是否不对易必须定义算子再检验。

## 2. 分工与最小交付

| 角色 | 负责工作 | 写入边界 |
|---|---|---|
| 总管 | 计划、监督设置、任务下发、结果裁决、状态与阶段同步 | 本计划、CURRENT_STATUS；理论稿交接后才整合简报 |
| 理论 agent | 简化传递关系、极限、反例、最接近原始文献；提前写留出预测；独立核查数值解释 | 首轮 science_brief.md 的理论与预测部分，不修改数值代码或结果 |
| 数值执行任务 | 复用现有接口，有限计算、必要验证、可复算输入输出 | 新增一个试验 runner、首轮 numerical 子目录；不修改生产源码或正式验证代码 |

项目内首轮目录为 results/paper2_science_pilot/v01_20260905；执行前确认不存在旧同名成果，若有则先读状态避免重复运行。
理论与结果共用一份 science_brief.md；数值 agent 只写 numerical 中的数据与简短运行说明，由总管在理论 agent 完成交接后整合。
允许用 apply_patch 迭代本轮新建的未封存代码和工作稿；已经产生的计算结果、预测截点及旧证据不可覆盖。
不额外建立角色阶梯、封存流水线、全仓哈希清单、通用平台或记忆文件；本任务无系统记忆更新授权。

输入：src/paper2_hybrid 现有模型和 numerics；Figure 1 theory contract v02；P5 r03 预检记录。
生产 API、默认参数、正式 Figure 2 阈值均不修改。用独立配置调用 build_system 与 numerics.simulate_endpoint，并沿用 endpoint_structural_gate。
现有 harmonic 解包含时间离散，不得把离散频率响应称为连续时间解析真值。不得把随网格变化的链节点数当作真实细胞数效应。
本轮 S1 是探索对照，不再是未见数据；正式 Figure 2 若要求 S1 盲留出，日后必须披露本轮接触并重新设计前瞻留出，不得沿用“未见”标签。

## 3. 首轮执行设计（最多 30 个工况）

先推导可区别的预测与明确反例，同时允许数值 agent 准备 runner 并开展非留出基础计算。
两工作日是工作预算，不承诺阳性结果。默认每次最多 1 CPU、8 GiB，无 GPU，串行计算；累计计算墙钟超过 7200 秒先停止并由总管重估，不自动扩额。
使用已验证本地 FEniCSx 镜像和运行方式；工作代码只读挂载，仅本轮输出目录可写，容器缓存放内存临时区；不拉取新镜像、不联网、不挂载 Docker socket。

固定 L、T、h_m/L、材料比、泊松比、界面刚度、支撑、阻尼、激活幅值及空间对比度。
De=τ/T，只改变 τ；H=h_e/L，只改变 h_e。不是扫描频率，不能据此声称完整频谱规律。

| 分组 | 配置 | 数量 |
|---|---|---:|
| 基础 | De={0.02,0.2,2} × H={0.1,0.3,0.6} × A1(uniform)/S1；S2、T128 | 18 |
| 空间复核 | (0.02,0.1)、(0.2,0.3)、(2,0.6)，S1；S3、T128 | 3 |
| 时间复核 | 相同三个点，S1；S2、T256 | 3 |
| 留出 | (0.07,0.2)、(0.7,0.45) × A1/S1；S2、T128 | 4 |
| 零驱动 | 中间点 P0；S2、T128 | 1 |
| 半幅驱动 | 中间点 A1，a0 从 0.10 改为 0.05；S2、T128 | 1 |

留出不得提前计算、读取或用于调整解释。理论 agent 先将预测写入简报并通知总管；总管在首次留出前记录该简报摘要值，随后明确释放这 4 个工况。
若先发现明确无新意或数值失败，可提前停止；30 为上限，不是必须凑满。重试消耗计入总预算，不建立额外扫描轴。

观测量：整体短缩、局部心内膜应变和两界面牵引的时间基频幅值/相位与空间分布、循环主动输入功、ECM 黏弹与其他耗散及其分配。
报告原始输入功；相同平均激活不冒充相同输入功。派生比值必须说明分母；近零幅值相位标为不可解释，不伪造为零。
每工况保存实际参数、观测量、结构检查、运行耗时及代码版本。保存重算所需数组；不把大数组加入 Git。

## 4. 验证、失败与科学裁决

保留现有残差、功率闭合、作用反作用及非负耗散检查；不放宽阈值制造通过。
零驱动检查零响应；半幅驱动应为状态/牵引幅值 1/2、功与耗散 1/4，在既有残差和浮点误差范围内比较。
三个代表点的幅值相对变化 ≤5%、相位差 ≤5°，仅为探索性分辨率标准，不替代正式数值认证。
近零量使用有单位的绝对量和误差，不计算失真比值。候选效应必须与实测数值误差并列；低于或接近误差的不作为机制证据。
快/慢松弛、薄层、均匀场的理论极限需说明固定量与 DC/周期分量的区别。

- 继续：明确预测经数值及留出支持，且不能仅由经典滤波或平均运算解释；总管再下发有限稳健性验证。
- 收缩：结果可靠但只是已知规律的应用；收缩的是当前科学主张，不自动降低目标期刊。
  保留结果，转向与研究目标一致、具有可区分预测的最小新假设，不靠堆复杂度抬高定位。
- 暂停该主张：效应随分辨率消失、对照不公平或被理论否定；保留失败，解释原因，不自动加流体/非线性。

独立核查以重推关键关系、极限和观测量解释为重点，不将工程检查数量视为科学进展。

## 5. 监督、同步与后续路线

复用 prl-paper-2 heartbeat，绑定本总管任务，每 10 分钟检查数值任务与本轮交付；不重复派发运行中的任务。
每轮向用户报告科学问题、完成/计划工况、证据、阻碍、资源和下一动作；无变化也如实一行汇报，不制造百分比。
连续两轮无可核实进展先检查原因。普通有限诊断由总管安排；保持研究目标的理论创新按下述追加授权自主推进。
理论输出已交接且数值任务运行时，不再要求其反复输出计划或等待旧 C1 门。
每个科学里程碑精确审查并提交/非强制同步已授权工作分支；排除无关空间验证合同修改、旧 untracked、缓存和原始大数组；远端分叉时停止同步。
删除、项目外新建文件、GPU、盘符映射和不可逆外部操作仍须人类事先确认。不存在本轮清理授权。

有足够证据后按“机制确认 → 正式数值可信度与有限稳健性 → 理想心室 → 实验校准与独立验证”升级。
流体按规定腔面载荷、单向流动影响、必要时双向流固耦合递进，每一级必须回答上一级无法回答的问题；本轮不执行这些扩展。

## 6. 2026-09-05 用户追加授权：自主理论创新与固定期刊目标

用户在下发执行后明确补充：总管有权探索新理论结果，只要研究目标不变，可边执行边思考理论创新；随后再次指定目标期刊 Nature Physics。
因此，总管可自主提出、比较、否证和修订理论假设、约化模型，以及在结果支持下设计下一轮有界试验，不为每一次理论创新请求人类审批。
首轮 30 工况、留出释放和资源上限不因此扩大；后续数值扩展由总管先记录新假设、最小方程变化、对照、否证判据和预算后再下发。
继续区分已验证事实、理论推论、候选新机制、理想体预测及生理证据。新理论不预设必须非线性、必须出现相变或必须加入流体。
需要人类决定的是研究目标/目标期刊变更，以及原有安全例外或有实质外部成本的新行动；在既定目标内的理论路线探索不属于逐步人类审批事项。

## 7. 首轮 26 例后的科学决定（2026-09-05）

首轮非留出已完成，执行记录见 `results/paper2_science_pilot/v01_20260905/numerical/run_notes.md`，
独立理论审查与总管裁决见同目录上一级 `science_brief.md` 第 10–14 节。
有近似宏观运动不变而局部界面峰变化的观察，但仍未排除输入功、峰位与经典传递；一项空间检查超门。
当前创新主张暂不成立，不更改 Nature Physics 目标。4 个留出保持未运行，不为完成工况数量而释放弱预测。
下一步只追加一次最多 45 分钟的理论筛查：等 DC/基频整体运动、等主动功条件下，非负且有界的激活能否产生有限局部负荷差；
必须同时审查半正定功矩阵的零空间、幅值可行域、观测单位及经典被动网络反例。
理论 agent 只续写同一简报，数值任务保持完成状态；本阶段新增模拟授权为 0。
若结论只是经典线性可观测性，保留否定结果并自主提出一个目标内可证伪的新问题，不自动加入物理复杂度或扩大扫描。

## 8. 双约束反例后的机制归因（2026-09-05 监督更新）

纯理论续查已交接；无 ECM 双支路反例说明等整体运动、等功局部差本身不具体系特异性。
下一步不增加 FEM，不批准尚未明确多端口匹配条件的集总 SLS 优化。
沿用同一科学简报第 17 节：对既有 18 个基础工况做同点同分量、原始/等功归一化和全场幅值对照；
同时核查集总对照可实现性与有限厚度经典解释。最多 30 分钟后处理、45 分钟理论工作，后处理计算累计最多 120 秒、1 CPU/8 GiB，无 GPU。
这是旧数据分析，不是新的独立验证，不改变首轮 26/最多 30 的计算计数；4 个留出保持未运行。
只增加必要的可复算后处理入口和小型 JSON，不改生产接口、不绘正式论文图、不重跑已有模拟。

## 9. 场比较验收后的下一理论问题（2026-09-05）

第 8 节两项任务完成。等功后全场差异仍在，但最大倍率依赖低 A1 基线；不是等整体运动，也不是新颖性证明。
详见科学简报第 19 节。当前集总容量优化停止，不把精确消元恒等式当作否定一切线性创新的理由。
下一步仅核查简报第 20 节提出的物理候选：主动心肌与被动 ECM 的横向变形是否互相抵消，形成可预测的均匀驱动牵引低谷。
理论 agent 最多 45 分钟、一页级推导；必须独立闭合轴向响应、说明弱法向应力近似和支撑/drag 边界、区分几何抵消与新规律。
候选来自训练结果，不能当先验；新增 FEM/优化/扫描为 0。4 个留出保留，数值任务暂不下发新计算。

## 10. 第二批：横向抵消位置律的 8 端点定向验证（总管授权）

总管依据用户第 6 节持续自主权限，接受简报第 21 节为可检验的约化候选，另行授权本批；不是接受创新成立。
首轮已在 26 个完成工况处收缩，原 4 个留出继续锁定，不借用、替换或重标。第二批单独计数，最多 8 个端点，
不得把两批合计 34 个写成首轮不超过 30；若本批完成，两批实际已计算为 26+8，保留留出仍为 4。

- 问题：源参数预测的 H≈0.260 低谷是否出现在 A1 上界面 y 牵引空间均值，S1 第一空间模态是否保留？
- 只固定 De=0.2、T128、原激活幅值和所有材料/界面/支撑/阻尼；A1 在 H={0.210,0.260,0.310}、S1 在 H=0.260，
  四个物理条件各有 S2、S3，合计 8。比理论建议多的两点仅补齐两侧粗网格误差，避免错误转用中心点误差。
- 新批计算累计墙钟上限 900 秒，串行最多 1 CPU、8 GiB；原项目累计 7200 秒停止重估规则仍有效。代码准备超过 45 分钟先回报。
- 复用现有生产 API 与旧 runner 的无副作用提取函数；不修改 src、旧 runner、原结果、正式 Figure 2 判据，不新增物理/时间轴/优化/图。
- 新入口仅 `scripts/run_paper2_transverse_notch_v01.py`，新输出仅 `results/paper2_transverse_notch/v01_20260905/`，已确认两个目标不存在。
  沿用既有本地镜像、只读代码/只读根文件系统、仅结果目录可写、临时缓存内存、无网络/无 GPU，不创建盘符或项目外文件。
- 完整预登记量、PASS/FAIL/INCONCLUSIVE 规则与解释边界见科学简报第 22 节。执行前先提交预测，manifest 记录预测文件 SHA256 与对应提交。
- 任一非有限数组、结构/功率闭合/耗散失败或超预算立即保存失败并停止后续端点；不沿用旧 runner 的结构失败继续循环行为，不自动补跑。

本批只决定是否保留位置律。结果失败/不确定时不改门槛或补点，也不自动跑原留出；由总管先解释偏差。

### 10.1 仅启动配置修复的一次重试（总管授权，预测不变）

首次启动在导入阶段失败，0/8 FEM 端点；`v01_20260905/summary.json` 为 ABORTED_FAIL_CLOSED。
总管独立核对失败容器及镜像：前者 PYTHONPATH 仅为 /workspace/src，覆盖了后者的
`/usr/local/dolfinx-real/lib/python3.12/dist-packages:/usr/local/lib:`，因而找不到 dolfinx。
这不是科学阴性结果。原预测提交 9842b3bfab7f11e947d0c355fa8b0202a368138f 和简报
SHA256 097c60e179136f02c30511a5f60e7fd5f852b63df1038641e1cce46b1f7465f2 保持不变；本次不编辑简报。

依照常规修复自主权限，总管只授权一次 v02 重试，覆盖此前“不得自动重试”的停止点，不扩大八端点范围：

- 失败目录、container.cid、summary 和 v01 入口原样保留，不删除或重用失败容器。
- 新入口 `scripts/run_paper2_transverse_notch_v02.py`、新输出 `results/paper2_transverse_notch/v02_20260905/`，
  固定新容器名 `prl-paper2-transverse-notch-v02-20260905`；两个文件系统目标已核验不存在，启动前还须检查容器名未占用。
- 仅允许从 v01 做最小版本修订：输出/容器/入口身份、启动依赖检查、预算扣除及不会涉及删除语句的内存释放写法。
  工况、提取/空间投影公式、科学判据、源物理和冻结预测必须逐项相同；不重构平台。
- 镜像原 PYTHONPATH 必须保留，实际启动采用
  `/workspace/src:/usr/local/dolfinx-real/lib/python3.12/dist-packages:/usr/local/lib:`。
  在同一个正式容器执行任何 FEM 前，验证 dolfinx 与 paper2_hybrid 的实际导入路径，形成对本错误的直接回归检查，
  而不是只再跑 AST 测试。读回容器实际环境；若镜像原路径与本节不一致则停止，不猜测路径或装依赖。
- 保持同一镜像、无网络、只读根/项目、仅 v02 输出可写、内存临时区、1 CPU/8 GiB、无 GPU。
  原批900秒预算不扩额，先保守预扣失败启动3秒，重试计算最多897秒，外层启动与运行也受剩余时间约束。
- manifest 区分“冻结预测提交”和“当前运行源码/入口版本”，并记录失败尝试路径及3秒预算预扣；不得把新 HEAD 误当作预测漂移。
  任一启动或结构检查失败，保存失败并停止，不再自动创建 v03；成功后正常完成原8点并交总管。

本条是执行配置修复，不是对科学假设、参数、数据解盲划分或验收门的修订。仍不做图、不提交推送，由总管统一验收。

## 11. 第二批验收后的单一步骤：无拟合的法向均值传递闭合

第二批 v02 已完成 8/8，两门 PASS；总管独立复算及理论 agent 只读核查同意保留三点均值低谷候选。
验收详见简报第 23 节。A1 中心仍有显著非零空间模态，约化厚度响应尚不能解释牵引低谷深度；
当前先回答这个物理缺口，不继续扫描。Nature Physics 目标不变，尚未形成该水平的创新证据。

下一任务由同一理论 agent 完成，最多 45 分钟、同一简报追加一页级第 24 节（建议不超过 150 行）：

- 从生产方程和界面牵引定义独立推导 x 平均的层内法向平衡，保留复模量、有限底部法向支撑、
  两界面弹簧和顶链 drag；明确周期侧边项何时消失，不能假设弱法向应力或顶面位移等于厚度变化。
- 目标是以整体复应变与平均激活为两个输入，得到上界面均值牵引的无拟合传递式；
  区分“已观测整体应变下的条件预测”和“仅源参数的自主预测”。保留宏观支撑/drag 的 x 加权位移项，
  不把条件一维闭合冒称全系统一维等价，也不据条件恒等成立认证创新。
- 核对符号、维度、零驱动、零 drag/硬界面等极限；给出仅用现有 8 例即可检验的最小计算表达和偏差归因。
  本阶段只允许解析/小矩阵公式的少量数值求值；不运行 FEM、优化、参数扫描，不新增物理或通用工程。
- 先交公式和否证条件给总管；总管复核后才安排数值任务对既有数组作有限后处理。此时不提前读原 4 留出。

理论 agent 与总管分别独立计算第 21 节源参数式：心内膜轴向刚度从 0.8 改为 0.6/1.0 时，
预测 H_min^(0) 分别为 0.19751726/0.32337460，复零点为
0.19271394+0.06341440i / 0.31640220+0.09060426i。
这是将来可能检验的左移/右移方向预测，尚未授权相应 FEM，也不作为当前位置律的新增验证。
先解释现有观测，再决定是否冻结此挑战的端点与阈值；不因预测看似合理自动扩大工况数。

本阶段数值执行任务保持已完成；已完成 FEM 总数 34，原 4 留出未运行。总管负责本批精确提交与非强制远端同步；
保留失败、排除 NPZ/旧临时产物及无关 Figure 2 合同修改。监督继续每 10 分钟，下一门为条件传递推导验收。

## 12. 条件传递验收后：复算已有数据并检查经典解释，不新增 FEM

第 24 节推导已交接；总管从原始数组和增广矩阵指数独立复算 8 例，与理论 agent 的 JSON 求值一致。
条件映射可接受，但 A1 低谷的宏观微小近似误差被相消放大；直接套用 chi0 到 S1 时相位差约 50.7°，
不能把条件准确性冒充对异质驱动的自主预测。验收与阴性项见简报第 25 节。

本阶段只下发两个互补的小任务，不建立新的角色或报告树：

- 数值执行任务：最多 30 分钟准备、120 秒累计后处理，串行 1 CPU/8 GiB，无 GPU。
  只读取第二批 v02 的 8 份 JSON/NPZ，保存无拟合的条件传递、chi0 源参数预测、复残差/幅值/相位差、
  相消条件数与双网格差；A1/S1 全部保留，不能只输出成功点。复用已有记录结构，仅增加
  `scripts/analyze_paper2_mean_normal_transfer_v01.py` 和
  `results/paper2_transverse_notch/v02_20260905/mean_normal_transfer_v01.json`，启动前确认目标不存在。
  优先简短单文件计算；不重建 FEM 系统、不修改生产 API/默认参数/旧结果，不新建通用框架或论文图。
  用小矩阵传播公式，从原始时间序列提取输入和实际 c0；对比 JSON 定义以避免反号或归一化错误。
  记录直接输入摘要、公式版本和运行耗时即可，不做全仓审计。非有限值、输入身份不符或预算超限立即停止报告。
  这些是已见数据的诊断，不创建新的 PASS 门；近零幅值不得给失真的相对误差/相位。
- 同一理论 agent：最多 45 分钟，用公开通用概念检索并实际阅读 3–5 篇最接近的原始论文，
  区分双层本征应变抵消、有限厚度传递、惯性反共振与当前过阻尼问题；向总管回交一页比较和唯一优先问题，
  不向外发送未发表项目内容，不以检索未命中证明创新，不写新文件，不运行 FEM/扫描或读取留出。

总管验收后再决定是否研究牵引驻点的边界刚度位移，或先收缩现有主张；Nature Physics 目标仍固定。
本轮新增 FEM=0，已完成总数仍为 34，原 4 留出锁定；累计 7200 秒停止重估规则不变。

## 13. 位置律的理论可检验性：先给驻点和近似误差容限

第 12 节两项任务均完成。总管已接受可复算后处理，并记录五篇原始文献及实际读取边界；详见简报第 26 节。
当前经典要素不足以支撑创新主张，保留的是一个待检验的位置规律问题，Nature Physics 目标不变。

同一理论 agent 的唯一下一任务，最多 45 分钟、同一简报追加一页第 27 节（建议不超过 150 行）：

- 固定原 De=0.2、T128 等源参数，仅理论代入心内膜轴向刚度 K_nx={0.6,0.8,1.0}。
  从第 24 节完整法向传递和明确标为近似的 chi0^approx，求
  c0_src(H,K)=[p_a(H)-p_epsilon(H)chi0^approx(H,K)] a_bar 的幅值驻点，不能用第 21 节厚度变化驻点替代。
- 允许在 H=[0.1,0.45] 内最多三次标量驻点求根、合计最多 120 次小矩阵/导数求值，计算最多 120 秒。
  报告端点及驻点性质；未能排除多个驻点就只报局部候选，不宣称全局最小值或连续位置已认证。
  不调用 FEM、构建系统、优化激活形状或扩大参数网格，不读原 4 留出，不拟合已有 c0。
- 对候选中心及 H_center±0.03 两侧，给出源预测幅值差和允许的宏观闭合误差条件。
  利用固定传递参数时 delta c0=p_epsilon delta epsilon，推导“中心仍低于两侧”所需的误差上限；
  必须区分独立可证明的界、仅从已见数据估计的误差、尚待检验的假设，不把训练残差当跨刚度保证。
- 只交接一页公式、三个预测值和一个明确的下一数值判定建议；不能因标量求根成功就自动下发 FEM。

数值执行任务本阶段已完成，不重复运行。总管同步本批成果后等待推导交接；是否开展新前瞻批次由下一具体决定给出。
本阶段新增 FEM=0，累计完成仍为 34，原 4 留出未释放；不增加流体、非线性或工程平台。

### 13.1 总管固定点独立复核

理论 agent 已交接第 27 节，共使用 118/120 次传递求值。该次求根任务结束，不继续搜索。
总管依据持续自主权限另行安排九个已给定厚度点的独立指数传播复核，最多 10 秒、不求新根、不读取新 FEM 数据。
这九点另计，不能继续报告所有理论求值总数仍为 118/120。重点核对幅值、解析导数与误差容限；
并修正“充分条件不满足就否定预测”和“S3-only 使用 1e-12 作为 FEM 误差”的建议，之后才决定是否授权新批次。

## 14. 第三批：两个新边界刚度的 12 端点前瞻检验（总管授权）

总管已独立复算第 27 节九个固定点并接受局部候选；经理论 agent 独立核查，冻结的科学门与纠错见简报第 28 节。
依据用户持续自主权限，现另行授权此批，不以模型变复杂为目的：唯一问题是源参数能否提前给出彼此分离的牵引低谷窗口。

- 仅 K_nx=0.6/1.0，分别使用简报第 28.2 节三个固定厚度，各 S2/S3、A1、T128、De=0.2，共 12 个端点。
  K=0.8 不重跑，原四留出不运行；已完成 34，本批完成后总数最多 46，单独计数，不冒称首轮仍不超过 30。
- 新入口仅 `scripts/run_paper2_impedance_shift_v01.py`，新结果仅 `results/paper2_impedance_shift/v01_20260905/`；
  两目标已核查不存在。复用已有生产求解接口和无副作用的观测/理论提取函数，不改 src、默认参数或旧 runner/结果。
  新入口只需串联 12 个配置、提取量和固定门；不重建通用框架，不增设规划/审计角色或额外报告树，不自动作图。
- 准备最多 45 分钟，串行 1 CPU、8 GiB，本批求解累计墙钟最多 900 秒，项目累计 7200 秒停止重估规则保持。
  已记录前两批 FEM/失败预扣为 104.4488 秒，后处理与小矩阵理论计算另列，不能将其与全部项目工时混同。
- 沿用本地镜像 `dolfinx/dolfinx:v0.11.0`，镜像 ID 为
  `sha256:2ae4bfbc0d9077268880faf04c72750528bee986c94ab223a2c159969bd56fa8`，本轮已核验存在。
  使用唯一新容器名 `prl-paper2-impedance-shift-v01-20260905`，启动前确认未占用；不复用/删除旧容器。
  根文件系统/项目只读，仅本批结果目录绑定可写，内存临时缓存，无网络/无 GPU，不拉镜像或安装依赖。
  保留已修复 PYTHONPATH `/workspace/src:/usr/local/dolfinx-real/lib/python3.12/dist-packages:/usr/local/lib:`，
  在同一个实际容器内先验证 dolfinx 与 paper2_hybrid 的真实导入路径，再计算，启动失败也保存后停止。
- 一个 manifest、一个 summary、每例参数/观测/检查/NPZ 即可；记录冻结预测版本、直接源码和 runner 身份、资源和耗时。
  数组不加入 Git；不把结构恒等式当独立测量。所有失败保留，不能改门、移中心、扩窗、补点或自动重试。
- 任何非有限值、生产结构/账本/耗散失败或预算超限，停止后续端点并交总管；没有执行配置修复的新授权时不得自动 v02。

执行前由总管提交预测，执行任务独立核对固定点与门后直接运行，不再增设与当前风险无关的预检阶段。
理论 agent 本阶段已交接，运行期间不改简报；任务完成由总管验收再决定下一科学步骤。

## 15. 第三批验收后：局部模态的锚定来源，只做理论核查

第三批已完成并由总管独立验收，详见简报第 29 节：12/12 端点完成，四个侧向预登记门 PASS，
实际总数 46，原四留出未运行。仅两个提前指定的离散窗口获支持，不能声称整个刚度曲线单调。
数值任务停止新算；按理论优先流程，本阶段不增加 FEM、留出、频率轴、流体或其他物理。

同一理论 agent 的有界任务已下发，最多 45 分钟，只读、消息交付约一页，不新建报告或改写数值结果：

- 独立核查两个窗口 PASS 的结论边界，保留宏观近似、非等功、局部非零模态和旧空间失败限制。
- 从现有总位移=周期涨落+宏观仿射应变的变换与源级组装，指出支撑/体阻尼/链阻尼对非零空间模态的驱动项；
  明确有限厚 ECM、锚定及均匀激活的不同作用。连续模型的对称性不直接冒充实际三角 P1/离散链恒等式，
  也不把有物理来源的边界效应直接称为数值伪影。
- 提出一个同一研究目标、现有物理内的最小可证伪理论预测或归因检查；本次不运行新 FEM、优化或扫描。
  不预设局部负荷主要由锚定或 ECM 主导，不把均值抵消、条件数或经典空间过滤本身当创新。

总管验收后再记录下一最小检验的具体范围。Nature Physics 目标固定，当前贡献尚待确立。
本批精确提交仅包含新 runner、参数/观测 JSON、容器身份及当前三份计划/简报/状态；
原始 NPZ 本地保留且不入 Git，无关修改和历史产物不纳入，不清理任何目录或容器。
