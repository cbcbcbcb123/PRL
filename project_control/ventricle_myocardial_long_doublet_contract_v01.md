---
status: frozen
execution_status: authorized
date: 2026-09-14
stage: Z1-MYO-LONG-DOUBLET-V01
---

# 长程双胞与扩展前资格

用户在短程修订passed后要求“然后你继续任务”，授权当前下一步CPU资格检查。关于形态不整齐，本轮先核对文献并形成有来源的采纳建议，不把未标定节点噪声或热噪声加入正式力学模型。无16胞动态重跑、GPU、安装、第二内核、参考形状膜能。

## 固定模型与矩阵

复用barrier_v01冻结可执行文件、END/SIDE初始+.070双胞网格及全部材料，两个细胞全自由、恒定骨架预应力，周期驱动关闭。此次算法坐标0至5，非生理时间。END/SIDE × dt上限.02/.01，共4条，按END粗/细→SIDE粗/细顺序。新结果create-only：results/ventricle_z1/z1_myo_long_doublet_v01_20260914。每步保存完整节点，关注0/1.25/2.5/3.75/5及首次停止/最后完整状态。

单线程CPU每条<=360s，总<=1440s，工作集及输出<=2GiB，单步<=30s，原内核2000步上限保持。新增外层几何监控每个已flush完整状态：体积误差>2%或最小角<15度即合作停止；与原原生硬门5%/8度不同，当前资格采用更严的既有验收线。STOP请求后允许当步完成，记录首次越界及最后完整网格。相邻共享边面最小法向夹角余弦需>-0.95，且闭合有向体积为正；这是折叠接近阈值，不是共享顶点扇面完全无交证明。禁止失败后自动改参数/延长预算重跑。

四条轨迹只有全部完整、间距>1e-8、无独立穿透见证、体积误差<=2%、角>=15度、相邻法向余弦>-.95、末态位移dt差<=2%方可判长程数值资格passed。末态最大自由力<=.001另判静态平衡；不能用有限时程终点冒充平衡。任一轨迹失败，后续长程矩阵not_run，不以“正间距”掩盖网格质量失败。

## 其他资格顺序

长程通过后才用新create-only诊断探针测试实际主动步长裁切和2/4/16胞静态接触开销（不是组织松弛），每例<=60s、总<=180s；性能先测量后判断优化必要性。若前置长程失败，本轮不进入这一分支。工程折叠监控用合成共边三角形正/反例自测并对保存状态复算，不更改原生产核心或冻结exe。

## 交付与证据

实际结构、结果/失败图、多个算法状态、逐胞尺寸、压力/曲率/接触牵引及独立诊断；定量沿用已确认10×5英寸轴框。所有旧结果保持不变，导航同步渲染/严格链接校验。原tmp/contact_barrier_v01_build已获用户删除批准，但本轮执行工具拦截删除，状态blocked、目录保留；不得改用其他接口绕过。
