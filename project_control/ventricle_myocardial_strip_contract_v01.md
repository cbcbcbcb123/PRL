# Z1-MYO-STRIP-L5-A 冻结执行合同 v01

- 冻结日期：2026-09-13
- 执行状态：`authorized`
- 计算类别：CPU 单线程、create-only
- 求解坐标：数值激活相位 `phi in [0,1]`，不是生理时间

## 冻结工况

| 工况 | 胞内收缩 | 固定界面链接 | 两端夹持 | 用途 |
|---|---:|---:|---:|---|
| `PASSIVE` | 关闭 | 开启 | 开启 | 零激活漂移对照 |
| `SYNC` | 五细胞同步 | 开启 | 开启 | 主要等长收缩场景 |
| `CENTER` | 仅中心细胞 | 开启 | 开启 | 界面力传递阳性对照 |
| `CENTER_NO_LINK` | 仅中心细胞 | 关闭 | 开启 | 界面力传递阴性对照 |

## 冻结几何与参数

- 细胞数：5；排列：`1 x 5` 长轴首尾串联。
- 单细胞输入：`results/ventricle_z1/z1_bioform_myo_r_v03_20260913/raw/FULL_M320_DT020` 的 snapshot 8。
- 初始界面间隔：0.20 模型长度单位。
- 每个界面固定材料链接数：由外侧 12% 端帽节点一一匹配，预期 7 条。
- 每个细胞胞内长轴收缩单元：同一端帽节点一一匹配，预期 7 条。
- 主动形态维持应力幅值：0.060；主轴比例 `[1,-0.15,-0.85]`。
- 最大主动缩短幅度：`epsilon = 0.05`。
- 激活函数：`a(phi)=sin(pi*phi)^2`。
- 弯曲项：关闭；参考边/参考面/目标终态形状项：0。
- 动态重网格：关闭，以保持材料链接身份。

## 必须保存

1. 模型结构图。
2. `SYNC` 至少 5 个明确标注的求解器相位形态图。
3. 曲率、压力、主动形态牵引、胞内收缩牵引、界面黏附牵引、总力牵引场。
4. 端部反力—相位、平均细胞尺寸—相位、界面力—相位和对照比较图。
5. 节点、面、状态、细胞、步进审计、源码哈希、命令和判定文件。

## 冻结门禁

### 数值门禁

- 所有保存状态有限；
- 每个细胞拓扑保持 162 节点、320 面；
- 最大相对体积误差 `<= 0.02`；
- 最小保存三角形角度 `>= 15 deg`；
- 固定端帽最大位移 `<= 1e-10`；
- 固定材料链接成对作用力相对平衡残差 `<= 1e-12`；
- 无链接工况固定界面链接数必须为 0；其余工况每个界面预期 7 条且全程不变；
- 每个过阻尼子步的功耗相对残差最大值 `<= 1e-10`。

### 机制门禁

- `PASSIVE` 峰值端部反力不超过 `SYNC` 峰值主动增量的 10%；
- `SYNC` 峰值端部反力相对其相位 0 基线增加至少 20%；
- `CENTER` 的中心激活必须在两侧端部产生可测反力增量；
- `CENTER_NO_LINK` 的中心激活端部反力增量必须不超过 `CENTER` 的 10%；
- `SYNC` 在峰值激活时五个细胞平均长轴尺寸不增加，且平均横向或厚度尺寸至少一个不减小。

## 判定词

- 全部门禁通过：`passed_synthetic_strip_mechanics`。
- 数值门禁失败：`failed`，保留失败包。
- 依赖或数据缺失：`blocked` / `blocked_data`。
- 实验比较始终另列为 `qualitative_consistency_only` 或 `blocked_data`；不得写成实验验证通过。
