---
status: frozen
execution_status: authorized
stage: Z1-MYO-SHEET-2D-M0
date: 2026-09-14
---

# 2D-M0 几何与真实接触资格合同

用户对二维几何与接触预检方案回复“同意，继续”，授权本次项目内 CPU 实施、实际检查、验证、绘图与导航同步。该决定取代上一轮“二维未授权”的前向状态，不改变旧失败结果。0 GPU，不安装、不删除、不在工作区外写文件。

G0：16 个相同六棱柱闭合 DCM，4×4 隔行错列；中心间距 x=9.2，行距 y=4.995，隔行 x 偏移4.6。单胞六边形顶点 (±4.6,±1.665)、(0,±3.33)，z=±2.65；平面内缩0.99形成正间隙。每面先定向三角化，再统一中点细化两次（384面），对照三次（1536面）。尺寸是合成几何值，不是生理标定；分区只用于初始网格，不加入目标形状能。中央ID为5、6、9、10。

G0门：16个闭合定向凸曲面；Euler=2，每边两次反向出现；体积正、三角角>=15度；每胞共面/凸性检查、所有120对胞的分离轴检查证明无体积重叠；预登记邻接图连通。报告初始接触间隙和投影孔隙，不以节点距离替代穿透检查。

C0：复用 external/simucell3d 当前编译 CONTACT_MODEL_INDEX=2 的实际接触分支。沿x端向和隔行斜侧向法向，分别扫有符号间隙 -0.035、+0.07、+0.70，捕获尺度0.35固定；每位置从独立未加载几何开始，分别用384/1536面，心肌类型4共12例。另跑类型0同矩阵12例作为代码语义对照，不能将其冒充心肌通过。adherence=0.04、repulsion=0.16；采用既有M0参数初始化法向和曲率后清空内力，再单独调用接触，不进行生理时间推进。

C0记录：调用前后节点位置、接触节点力、法向、曲率、内部压力、耦合节点数、完全耦合三角面的去重面积；后者是离散支持面积而非真实成熟黏附面积。双胞合力/合矩绝对值及贡献归一残差；两档法向合力变化。纯排斥/黏附力与直接几何投影必须区别。

C0门：超范围零力且零耦合；微压入的法向作用为排斥；正间隙必须有吸引传力或有可审计的接触约束/反力通道；作用反作用相对残差<=1e-10；网格细化法向力差<=5%。将“0力但没检查耦合”判为黏附通过、把类型0几何投影伪装成力学力，均禁止。若心肌通道没有黏附力也无耦合，C0 failed，组织静态维持not_run。

CPU预算：G0<=120s，24个静态接触调用及输出累计<=300s，单线程；编译预算另计。C0通过后才冻结静态松弛参数和预算，G0/C0失败不启动整片松弛。保存真实4×4结构图、几何门图、两方向/三位置的接触结果和网格场；位置扫描标为几何探针，不称时间或组织松弛过程。所有失败原样保留。

输出：results/ventricle_z1/z1_myo_sheet_2d_m0_v01_20260914/，create-only。生物学验证blocked，父Z1 blocked，主动二维收缩/ECM/心内膜不在本合同内。
