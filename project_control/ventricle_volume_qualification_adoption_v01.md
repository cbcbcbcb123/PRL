---
document_id: PRL-VOLUME-QUALIFICATION-ADOPTION-V01
date: 2026-09-18
status: passed
authority: user requested absorption of the supplied expert review and continuation
saved_state_analysis: authorized
new_FEM_batch: not_run
new_FEM_authorization: pending_one_confirmation_of_the_batch_below
---

# 采纳专家意见：一个局部体积资格问题，一个有界诊断批次

输入：[专家原件](../plan/active/EXP-20260918-FEM-review-v01/source/PRL_项目评审与提速建议_20260918.md)。
作者身份unknown；原件及用户随后补充的八成员ZIP均受控保全，数字先独立复核，不自动视作事实。
用户本轮允许参考吸收并继续。本记录落实流程与已有状态分析；涉及新的求解矩阵及停止规则，
仍按项目明确科学授权规则等待**一次整批确认**，不再拆成每个小工况逐次审批。

## 采纳矩阵

| 建议 | 项目处理 | 边界 |
|---|---|---|
| 保持FEM；暂缓FSI/ECM/DCM | 采纳，延续既定路线 | 不恢复DCM，不直接进入机制计算 |
| 分解J−1=p_m/κ+r_h | 采纳并独立复算 | 投影缺陷不是锁死/inf-sup失稳的证明 |
| 固定距离分区、RMS及体积权重 | 本轮执行已有状态离线分析 | 不删峰值点，不把积分点分区体积当连续精确体积 |
| 非零patch + 3网格×2κ制造解 | 采纳优先级，具体合同在下；待整批确认 | 先当前P2/P1，不直接移植二维DG |
| 一个薄层/去层界鉴别 | 保留，待小基准裁决后选一个 | 不同时扫描两种路线；不删除真实组织 |
| 胜出方案再回原心室 | 保留依赖顺序，未授权运行 | 原p=0.01、Ta=0、基底和1%门不改 |
| q05不能替代力学门 | 采纳为未来方案设计原则 | 不追认U1通过；原工程failed完整保留 |
| 独立诊断不必被精度失败全部阻断 | 纳入未来批次的明确停止规则，待确认 | 当前旧合同不回溯改写，不自动重试 |
| 探索用一页可重绘诊断，不做每微步投稿图 | 采纳本轮探索交付 | 一个PNG/SVG与脚本/数据；160 dpi明确标为诊断非投稿，原图不动 |
| 关反馈/开反馈与抑制-恢复机制 | 纳入后续设计问题，不实现 | 生长用F=Fe Fg，约束Je；无流体不得称WSS机制 |
| 公共数据和可辨识性可并行整理 | 保留，不阻塞当前最小诊断 | 本轮不追加下载或自造标定 |

## A：本轮已授权的保存状态分析（无新FEM）

只读D1包中M0/M1首压力态及几何；核心生产、原独立验证器与全部失败包不修改。
先复算原裁决，再增加以下描述量，不新增通过门：

- 积分点与原56额外采样点的max|J−1|、max|p_m/κ|、max|r_h|。
- 全域及每层体积加权RMS；弱压力矩、投影正交性与能量分解一致性。
- 超限采样单元的参考体积占比（不是连续超限区域精确体积分数）。
- 基底距离d=−Z/L：预先固定0.15、0.30、0.45，来自外壳高度1.5L的10%、20%、30%，
  与网格编号或失败单元位置无关。四区间为[0,.15)、[.15,.30)、[.30,.45)、[.45,∞)。
  另汇总d<0.30与d≥0.30，两个网格用完全相同阈值；原全域最大值始终报告。
- 分区RMS采用参考体积积分点权重；跨分区单元用积分点指示函数估计，不宣称精确截体积。
  额外采样点仅用于max和峰值坐标，不作为体积积分权重。
- 误差随距离的逐单元描述、真实参考结构/基底及固定分区线、同尺度M0/M1指标，一页交付。

create-only结果：`E:\Temp-Projects\PRL-results\ventricle_fem\volume_projection_audit_v01_20260918`。
来源共享只读指针+哈希，旧原始状态保留在原包；不为新页复制大型状态或Notebook图片输出。
预估新增32MiB，停止保全64MiB与磁盘10GiB余量；代码仓3GiB；单CPU、0GPU、无Docker/Gmsh。
仪表/复算不一致、输入漂移或无效J即停止本分析；原1%精度failed是预期可分析对象，不重分类。

<a id="B_pending_batch_confirmation"></a>

## B：待用户一次确认的八工况三维基准

这是具体新批次，不由专家原件自动授权；本轮不运行。保持μ=1、有限变形NH、P2u/P1连续p、
仿射四面体、六阶积分，复用现有生产本构/独立验证，而非复制第二套力学引擎。
必要的立方体/体力/边界牵引适配必须先通过代码与独立装配检查；不得改变原心室形式。

### 固定矩阵

单位立方体，确定性同几何四面体族；每轴n=2/4/8，对应48/384/3072四面体。
Dirichlet面X=0按准确位移施加，其他五面施加准确参考牵引t*=P*N；体力b*=−Div(P*)。
有限κ无压力规范自由度，不额外设置零平均压力。一个精确解对应一个从零初值的新SNES求解，
不自动添加延拓步或更换初值。牵引来自制造解，**不是**心室腔压。

| 工况 | 数量 | 精确位移/材料压力 | 网格与κ |
|---|---:|---|---|
| 非零均匀patch | 1 | u*=(.001X,−.0004Y,−.0003Z)，p*=κ(detF*−1) | n=2，κ=1000 |
| 非零剪切patch | 1 | u*=(.1Y,0,0)，J*=1，p*=0 | n=2，κ=1000 |
| 平滑制造解 | 6 | u*=(.002 sinπX sinπY sinπZ,0,0)，p*=κ(J*−1) | n=2/4/8，各κ=100/1000 |

制造解J*=1+.002πcosπX sinπY sinπZ，严格下界1−.002π>0。
P*由原能量解析求导，b*/t*由这个兼容P*构造，并以独立差分/解析检查载荷实现。
κ变化时p*、b*、t*也随之变化；这是兼容制造解敏感性，**不是固定载荷锁死试验**。
六工况不足以单独证明普适inf-sup稳定、无锁死或心室资格。

### 预先固定的评估与门

- 非线性/装配/力与矩平衡及正J安全门沿用可适用的原阈值，必须逐项说明边界面变化后的归一化。
- patch：相对位移L2/H1误差≤1e-8；压力L2误差/max(μ,||p*||)≤1e-7；
  max|J−J*|≤1e-9；各面总反力/牵引的归一化差≤1e-7。参考为解析可表示场，非零测试不被零载代替。
- MMS：报告相对位移L2/H1半范数、压力L2、J误差的RMS/max、r_h的RMS/max、反力、DOF及耗时。
  用至少八阶的独立评估积分；六阶生产积分保持不变，不复用求解器导出的误差标量当参考。
- 预期光滑稳定情况下u的L2/H1与p的L2渐近阶约3/2/2；本批拟议资格门为误差逐级下降，
  末两网格实测阶至少2.5/1.5/1.5，且n=8的三个相对误差分别≤0.02/0.15/0.15。
  未达门标记本批资格failed，不能由三层网格据此直接判定不稳定；可能尚未进入渐近区，另行诊断。
- n=8的J误差RMS≤1e-3；r_h报告而不事后加新否决线。所有max注明是采样，不作全域上界声明。
- 原心室max|J−1|≤1%继续保留，不用制造解误差门去追认原心室。这个参考解的真实J不等于1。

上述阈值是预注册的**拟议项目资格门**，不是通用定理/生物容许阈值；用户确认前可审议。

### 批次执行和停止

一次固定本地FEniCSx镜像容器、单CPU/8GiB/0GPU/禁网、最多8次SNES、每次最多30次Newton；
整批1800秒、预留120秒停止保全。预计新增256MiB+至少64MiB保全+10GiB余量，非固定存储限额。
只用原有运行时；不安装、更新、拉取或修复Docker；每个工况配置/初值/输出只登记一次，0自动重跑。

- 两个patch都是实现先决条件；任一失败，六个MMS均not_run，停止依赖链。
- patch通过后，六个MMS为相互独立诊断：单例精度门或普通非线性收敛失败保全后可做剩余预登记工况，
  不用失败解热启动其他工况、不追加新网格/参数。汇总保留每个failed。
- 输入漂移、接口/装配不一致、不可读状态、非正/非有限有效J、资源或写入错误：整个批次立即停止。
- 无三维主动、原心室重跑、薄层扩展、FSI、生长、ECM反馈、GPU、删除或推送。
- 交付一个表和一张轻量结构/误差—成本诊断页；保存所有原始数值/配置/状态/日志和可重绘代码。

本批结束后只裁决是否进入**一个**薄层鉴别，再决定同载心室回归；不自动推进后两步。

方法论参考：[Liu、Marsden与Tao的混合超弹性验证研究](https://arxiv.org/abs/1812.11216)。
仅参考其将弱约束、inf-sup和制造解分开检查的原则；论文的等几何方法结果不作为当前四面体对的稳定性证明。

## A执行结果与交付（2026-09-18）

离线分析passed，科学压力门仍failed，根因unknown；没有执行B或新FEM/Gmsh/Docker。
专家ZIP CRC/成员manifest/意见一致性通过，9份原件/附件原样保全；提供的Python脚本未执行。
用独立运动学自行复算，与专家8项指标在M0/M1均匹配至绝对差<1e-12。

| 指标 | M0 | M1 |
|---|---:|---:|
| 采样max\|J−1\| | 1.522743% | 1.449560% |
| max\|p_m/κ\| | 0.001722% | 0.002044% |
| r的参考体积加权RMS | 0.165124% | 0.120037% |
| 含超限采样点单元体积占比 | 4.526991% | 2.497692% |
| 峰值采样点距基底/L | 0.446464 | 0.301956 |

压力弱矩约1e-18且投影正交，但局部约束未满足。平均缺陷下降27.30%，不能被解读为局部合格。
固定d≥0.30L区域仍含全域峰值；不能以网格依赖的“基底邻接单元”证明误差只在固定面。
0.15≤d<0.30L区间RMS从0.2013%到0.2145%略升，保留此反例；不是各处均单调改善。
材料压缩项不足以单独解释局部偏差；薄层、基底、几何与压力空间贡献仍未分离。

交付：[单页结构及定量诊断](../../PRL-results/ventricle_fem/volume_projection_audit_v01_20260918/index.html)。
14项针对性数学测试passed；首次收口测试子进程因缺少PYTHONPATH未收集测试，
只补显式src路径后通过，`targeted_tests.json`失败与`targeted_tests_v02.json`通过均保留。
原D1全部127文件、9份专家材料及来源/代码快照哈希复核通过；派生JSON与数组精确读回一致。
图件目检修正页脚间距；只改画布与面板垂直位置，不改数据。160dpi探索图不冒充600dpi投稿终稿，
通用投稿风格检查的两项dpi失败原样记录，探索布局检查passed。
整包21文件、1,664,438 bytes（约1.59MiB）；manifest SHA-256
`78a9542bd69d6373a4e25f4bec421f6ea5441a49e4d5334932811201cd85628b`。
无删除/安装/外部服务/GPU/推送；本地main仅提交本轮明确文件，不纳入50项旧改动。
