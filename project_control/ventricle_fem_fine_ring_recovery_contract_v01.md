---
document_id: PRL-FEM-FINE-RING-RECOVERY-CONTRACT-V01
status: passed
accepted_at: 2026-09-18
authority: user 同意继续 to native interface diagnosis/minimal repair then only five fine states
execution_status: not_run
parent_execution: project_control/ventricle_fem_ring_passive_qualification_execution_v01.md
---

# F6-S1-S6：零载接口定位/修复与细圆环5态

仅执行已确认范围：定位上轮细网格零载原生崩溃，最小修复诊断接口，通过真实环境门后只补M1五态。
不重算M0，不改方程、材料、边界、网格、积分、压力步或原验收门；不扩展轮廓、主动、三维、FSI或生长。

## 有界诊断与科学门

1. 一次6自由度零右端SNES接口负例，使用固定镜像、原求解器选项及生产监测器；
   逐调用记录solve返回、KSP/PC/因子访问，尝试原无保护读取路径，启用Python故障回溯。
   该预声明负例用于定位，不是FEM重跑；故意暴露的旧接口错误保留为failed，不当作科学失败后自动重跑。
   若未定位到可证实且局部可修的边界，则停止科学运行，不凭猜测改材料或继续试算。
2. 定位后最小修复；生产诊断回调的协议回归先失败后通过。原生因子尚未建立时不强迫分解，
   本次0次Newton更新时不得报告陈旧因子的统计，因子状态not_run、数值null。
3. 一次修复后容器，先做6自由度真实接口检查：零初猜/零右端、非零右端、已收敛解再调用三种边界。
   任何非预期失败即停，不进入FEM；只有新因子实际存在的非零更新才回读MUMPS余量100和INFOG=0。
4. 接口通过后，仅M1零载及p/mu=0.02/0.04/0.06/0.08，共最多5次科学平衡。
   零载必须获得最终SNES报告、完整末态和独立物理门，不能以初始残量小代替完成。
   每态通过才进入下一态，保留全部实际Newton向量；首失败保存并停止，不自动重跑。

最多2个串行容器：负例定位限180秒；修复后接口及科学运行总限1200秒。二者均单CPU、8GiB、禁网、0GPU。
这是预先限定的负例/修复验收，不是失败后无限尝试；负例之外的任何运行失败均停止。

## 物理及原门限

沿用F6-S1-S5：P2/DG2，三维平面应变NH，mu=1、kappa=1000，Ta=0。
M1为128周向×径向2/2/10、3584三角形；内壁随动压力、外壁自由，A固定ux/uy、B固定uy。
SNES/线搜索/30步上限及MUMPS余量100不变。

所有求积点J>0且max|J−1|<=1%；独立自由力<=2e-6、弱压力<=1e-8、求解器残量<=1e-9。
F/J/应力/力重建、压力虚功、闭合力/矩、规范反力、DG2点态约束均保持上轮原门。
正压M1解析面积响应相对误差<=1%、径向位移L2相对误差<=1%；M0原2%门只读复核。
峰值粗细面积响应绝对差<=0.002且相对差<=5%；同网格旧CG1响应差<=5%。
两级通过只构成原数值资格，不是多级渐近/热点收敛或生物学验证。

## 保全和交付

create-only结果根：`E:\Temp-Projects\PRL-results\ventricle_fem\f6s1s6_fine_ring_v01_20260918`。
父包及祖先按哈希保护；复用M0五态，只复制必要对照与状态，不复制全部历史。
保存负例原始标记/日志、修复前后源码、容器设置、完整M1求解轨迹、独立核验、结构图及5个真实压力态场量图。
Notebook读取版本内物理复制输入；压力延拓不是生理时间。失败时不伪造缺失态。
结果准入：预计新增256MiB、停止保全至少64MiB、磁盘余量10GiB、仓库3GiB原门。
无安装/升级/拉取/Docker修复/重启/删除；所有既有无关修改保持。
阶段精确提交本地main，不推送；测试通过、工程恢复与科学资格分别报告。
