---
document_id: PRL-FEM-CONTOUR-ACTIVE-CONTRACT-V01
status: adopted
accepted_at: 2026-09-18
authority: user 确认执行 after the low-pressure active-contraction route adjustment
---

# F6-S1-S9：在已合格的低压态加入外层主动收缩

本决定仅替代S8合同“完整被动高压序列通过后才可提出主动切片”的前置条件。
S8的p/mu=0.08局部体积失败完整保留，不改写为通过。改用S7在两网格均已通过的
p/mu=0.02作为起点；本轮不重跑被动压力，不增加压力，不改材料或局部体积门。

## 冻结范围

- S8父包manifest SHA-256：58a708f93a3b3c0f290912054a19a00c3b17a9ec94aa2008247322416d6b3c21。
- S7合格父包manifest SHA-256：524b1560a9ba63a0b99a19f20188cdaf704bd05a0d507a65e32c1cf21ccf0704。
- M0先计算Ta/mu=0.025、0.05、0.075、0.10，M1再计算同一序列；全过程p/mu=0.02。
  最多8个新平衡，复用两网格零载/0.02态，不重新求解。前一混合解为下一态初值。
- 同一Ring内核，原3541/14164三角形几何，P2位移/DG2压力、六阶积分、有限变形
  平面应变Neo-Hookean，mu=1、kappa=1000，三层被动材料相同且未实验标定。
- 主动能量仅在layer=3：Ta/2 (|F f0|²−1)，f0=(-Y,X,0)/sqrt(X²+Y²)。
  这是既有内核中关于原点的环向假设，不是影像测量的纤维，也不是逐点轮廓切向。
  在心肌积分点检查参考半径非零；本轮不改变纤维方向模型。
- 内腔随动压力，外壁自由，A固定ux/uy、B固定uy。只有外轮廓来自72 hpf Fish 4图像；
  内腔/层界构造，不声称细胞形态场、三维心脏或实验复现。

## 验收与停止

原1% max|J−1|、正J、SNES/真实MUMPS分解、独立平衡、弱压力与DG2约束、
F/J/总应力/主动应力、固定点反力、虚功及主动层定位门均保持不变。
粗细网格同载荷总面积响应差绝对<=0.002且相对<=5%；这是旧门，不等于热点/渐近收敛。
主动增量相对于同压力零张力态的面积变化另行报告，作为模型响应，不事后调整门限。
收缩是否单调是科学观察，不能代替数值验收，也不能由期待结论强制制造。
首个求解/物理/粗细面积门失败即停止，保留失败态、最后有效态、真实Newton向量；
缺失态not_run，不自动重跑。零张力基线仍标记retained。

独立迭代审计仅扩展显式expected_activation参数，默认仍为0；不共享生产力学实现。
恢复前验证混合空间网格和DOF映射逐项相同。内核及独立力学重算函数不得修改。

## 运行与交付

create-only：E:\Temp-Projects\PRL-results\ventricle_fem\f6s1s9_contour_active_v01_20260918。
一次固定本地FEniCSx容器，1CPU/8GiB/0GPU/禁网，1200秒；无安装/拉取/运行时修复。
预计新增512MiB，停止保全64MiB、磁盘余量10GiB；代码仓硬上限3GiB。
保护S8及全部祖先、50项既有无关修改；不删除、不搬移、不修改冻结结果。
输出结构/边界/纤维假设图、真实加载态形变及总/主动应力和J图，真实1倍形变。
可执行Notebook、源数据副本、600dpi PNG、可编辑SVG、独立复核与视觉QA；
加载级不是心动时间。无主动零压对照、卸载周期、3D、FSI、生长或新增科学工况。
按已授权规则精确提交本地main，不推送。
