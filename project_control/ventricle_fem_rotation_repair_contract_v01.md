---
document_id: PRL-FEM-ROTATION-REPAIR-CONTRACT-V01
status: adopted
authorized_at: 2026-09-17
authorization: user agrees to repair initial lifting and complete only the missing rotation qualification
result: results/ventricle_fem/f3c_rotation_repair_v01_20260917
---

# F3-C｜可行初值修复与刚转补充资格

用户同意上一交付的下一步：仅修复非零位移边界的初值处理、补齐刚转验证；13个已完成工况不重算，F3-B原包及failed裁决不改写。只用FEM，不运行真实心室、腔压、生长或任何DCM。

## 固定修复边界

在现有Q2/Q1模块新增显式调用的`lift_dirichlet_initial(mesh, initial, dirichlet)`，逐位移分量用参考构形Q2离散Laplace算子延拓固定位移所需的增量。它是求解初值预测，不增加物理能量或弹簧，不更改材料、边界集合、压力初值、Newton方程、残差门或线搜索。内部节点依旧为自由未知量。

现有`solve`保持显式初值语义，不暗中修改用户初态。新旋转运行器先调用lifting再进入同一求解器。新预测必须满足给定边界且所有Gauss点J>0；不可行则失败停止，不自动改角度、减步或换方法。本轮只资格该结构化块的旋转预测，不宣称适用于任意解剖网格/任意边界。

开发回归须覆盖真正的“0°旧解→15°新边界”路径；原直接边界替换应复现初态翻转，新lifting应消除它。另覆盖不修改原向量、不改压力、非法输入拒绝与内部自由度保持。测试及诊断不作为额外生物学参数扫描。

## 冻结补充计算

父包`results/ventricle_fem/f3b_finite_strain_v01_20260917`，最终manifest SHA-256=`76167e61c4cdaf38011f6cd11ab4f329fa4d4439d9f3bf5a40dfc213acd79dc3`，列出62文件。启动前后校验所有文件及该manifest哈希。

复用原`nh_rigid_rotation`配置：2×1×1块、(2,2,2)网格、NH mu=1、kappa=1000、无主动张力/外牵引；125个位移节点、27个压力节点。98个外边界节点规定绕原点z轴转动，27个内部节点自由。

1. 从父包读取0°有效状态及history，不重新求解0°。按15°、30°、45°、60°依次lifting和求平衡，四个新增正式状态。保存前态、原先仅改边界的算术初猜和新预测，初猜必须与平衡态明确区分。
2. 在60°有效状态上增加一个固定的内部非仿射扰动：`du=0.002*prod_i sin(pi X_i/L_i)*[1,-0.7,0.5]`，长度单位1，所有边界节点的扰动严格设0，压力不扰动。用同一Newton求解一次恢复平衡。该辅助检查不改变物理载荷，验证算法确实能消除非零内部误差，不以解析初值零迭代替代求解器能力。

共四个缺失角度求解＋一个扰动恢复求解，一次有界正式阶段调用。任何失败立即保全、停止后续，不自动重跑。角度/加载进度不是生理时间。

## 固定验收门

- 原F3-B数值门不放宽：Newton归一自由残差<=1e-9、最多30次迭代、16次回溯；所有接受态J>0。
- 独立验证器不导入solver/material/runner作力学判断；重新构造Q2/Q1几何、F/Green/J、能量差分应力、平衡和边界。沿用F3-B场量1e-10、零应力绝对2e-7、独立自由残差2e-6、压力弱残差1e-8等门。
- 五个角度状态与解析刚转位移最大差<=1e-10，最大Green应变<=1e-10、|J−1|<=1e-10，最大P/Cauchy应力<=2e-7。
- 新预测的固定边界误差<=1e-10、压力逐项不变、独立Laplace自由残差<=1e-10、正J；0→15°旧boundary-only试猜应重新检出非正J，作为回归诊断而非新平衡态。
- 扰动初态偏离目标位移>1e-4、所有J>0，独立初始残差>1e-9且与history相差<=2e-6，至少一次接受的Newton更新；末态满足同一刚转门。与60°未扰动平衡态比较时，位移采用1e-10，压力采用原应力的2e-7门，不把混合向量中不同物理量套用同一位移绝对容差；末项history与保存Newton残差应一致<=1e-14。此分量解释在正式运行前冻结。
- 原13个完整工况及材料点/成对网格/近不可压子门依靠被哈希锁定的保留证据复用，不伪称重新运行。组合资格只有“原有效子门＋新刚转＋扰动恢复”全部通过才passed；原F3-B自身保持failed。

## 输出和资源

新包create-only，保留全部五角度状态（0°注明来自父包）、四组预测/旧初猜、扰动初末态与Newton日志、独立验证、来源哈希和精确代码补丁。至少输出模型结构图及真实五状态/残差结果图，使用既有统一风格，600dpi PNG与可编辑SVG，不补造求解帧。

单线程CPU、0 GPU，正式调用300秒、0自动重试，阶段上限16 MiB＋64 MiB停止保全、项目3 GiB。持有既有科学任务锁；启动前确认没有其他PRL科学worker。无删除、安装、提交、推送或项目外新建文件。后处理失败只能修显示/报告，不能借此重算科学状态。
