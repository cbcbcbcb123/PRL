---
document_id: PRL-FEM-CONTOUR-ACTIVE-EXECUTION-V01
status: passed
completed_at: 2026-09-18
contract: project_control/ventricle_fem_contour_active_contract_v01.md
scientific_scope: two_mesh_2d_quasistatic_active_response_only
biological_validation: not_run
---

# F6-S1-S9：低压下心肌主动收缩通过，缩腔幅度约1.85%

用户确认后执行：保留S8高压失败，改从两网格已合格p/mu=0.02态研究主动收缩。
M0/M1分别增加Ta/mu=0.025、0.05、0.075、0.10，共8个新平衡全部接受。
无载/受压零张力共4态只读复用；没有重新求解它们或任何高压态。

## 结果

下表缩腔量以**同压力、零张力基线**为分母；不是相对于无载参考态。

| Ta/mu | M0腔面积变化 | M1腔面积变化 | M0局部max\|J−1\| | M1局部max\|J−1\| |
|---|---:|---:|---:|---:|
| 0（复用） | 0% | 0% | 0.323434% | 0.358062% |
| 0.025 | −0.481951% | −0.481486% | 0.328390% | 0.350969% |
| 0.050 | −0.950012% | −0.949078% | 0.333202% | 0.343949% |
| 0.075 | −1.404771% | −1.403364% | 0.337869% | 0.337001% |
| 0.100 | −1.846775% | −1.844892% | 0.342390% | 0.330124% |

最高张力粗细收缩响应仅差0.001883个百分点。相对于无载参考态，腔室仍分别
扩张17.839020%/17.889685%。结论是**主动张力使腔面积单调减少，但目前只是部分抵消压力扩张**，
不宣称已出现实验幅度的强收缩、真实心动周期或生物学验证。

73项独立汇总检查passed，全部逐态力学门passed。粗细总面积响应差0.000506650842，
相对0.283208%，满足原绝对0.002与相对5%门；不是应力热点或渐近收敛证明。
最高张力细网格独立自由力残量约6.393e−14、DG2点态体积约束误差约1.193e−12。
所有状态局部体积偏差小于原1%门，主动应力只在构造心肌层出现；门限未改变。
保存态二次边界24/48段采样全部passed；这不是精确曲线全局单射证明。

## 模型、改动与边界

仍为原3541/14164三角形、二维P2位移/DG2压力、六阶积分、平面应变有限变形NH。
mu=1、kappa=1000、三层相同被动参数，未实验标定；只有外轮廓来自72 hpf Fish 4图像，
内腔/心内膜/ECM/心肌界面为构造。内壁随动压力0.02、外壁自由、A固定ux/uy、B固定uy。

仅layer=3使用既有主动能量Ta/2(|Ff0|²−1)，
f0=(-Y,X,0)/sqrt(X²+Y²)为**关于坐标原点的环向假设**，不是实测纤维或轮廓逐点切向。
两网格心肌积分点最小参考半径0.416192/0.415472L，未触及纤维奇点。
生产Ring内核、几何核和独立力学重算模块的SHA与S8相同。
只增加有界主动加载/审计/绘图；迭代审计的expected_activation必须显式给定，默认0保持。
运行器和独立验证不共享生产力学公式。

S8的0.08高压局部J=1.173743%失败保持failed；改变执行顺序不抹除这一适用范围限制。
载荷级和Newton迭代都不是生理时间。三维/FSI/生长/实验验证仍not_run。

## 运行、图件与完整性

- 一次201.218秒固定本地FEniCSx容器；1CPU、8GiB、禁网、0GPU、0自动重跑。
- 32个新Newton向量、8个新平衡及其输入链保存；140正式调用文件、41输入/源码副本核验保持。
- 1518父/祖先文件及50项既有无关修改保持；无删除、搬移、安装、拉取或Docker修复。
- 151测试+21子测试运行前passed；新增图件回归后152测试+21子测试passed。
- cb-paper-figure-workflow与cb-plot-unified-style：两套独立可执行Notebook、600dpi PNG、
  可编辑SVG、原始u/p/网格副本、显式样式例外与执行后目检通过。
- 结构图含层、固定点、压力与假设纤维方向；结果图含总/主动应力、J、面积响应；
  五态图为实际1倍形变、统一色标、不插值成心动时间。
- 宿主/容器独立报告一致。只复用既有有限差分舍入比较规则，没有再修改比较容差或物理门。

外置包：`E:\Temp-Projects\PRL-results\ventricle_fem\f6s1s9_contour_active_v01_20260918`。
201文件、350,554,617 bytes（约334.315MiB），结果不进入GitHub。
根manifest SHA-256：`80b0ecbe6bd35e6d00479fbeb1e424c89969e862a01bdb0885bb43dab9734c2b`。
本次主仓验收时约2.04GB，低于3GiB；本次临时目录保留，无清理授权。

[结果入口与两套图件](../../PRL-results/ventricle_fem/f6s1s9_contour_active_v01_20260918/index.html) ·
[独立复核](../../PRL-results/ventricle_fem/f6s1s9_contour_active_v01_20260918/post_verification.json) ·
[交付验收](../../PRL-results/ventricle_fem/f6s1s9_contour_active_v01_20260918/delivery_audit.json)

只读复核：`python -B -X utf8 -m prl verify fem-fenicsx-contour-pressure --active-at-qualified-pressure`。
运行入口create-only，不允许原包重跑。图件从包内副本重画到新版本，不需FEM求解器。
按长期授权直接本地main提交；没有推送或另开分支。

## 唯一下一步（待用户确认）

进入理想化三维心室固体：建立可解释几何、网格和纤维方向，然后验证低压与主动加载。
不再无限扩展二维参数试算；暂不加血流、生长或ECM反馈。
三维离散必须重新做资格检查，不能直接继承二维DG2的通过结论。
当前合同已完成，没有剩余科学运行授权。
