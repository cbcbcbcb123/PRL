# Z1-MYO-STRIP-DEFORMABILITY-HI-A 定向收敛修复合同 v02

- 日期：2026-09-13
- 基础失败包：`results/ventricle_z1/z1_myo_strip_high_amp_deformability_v01_20260913`
- 新建修复包：`results/ventricle_z1/z1_myo_strip_high_amp_deformability_repair_v02_20260913`
- 修复类别：CPU、两条单线程轨迹并行、create-only

## v01 冻结失败

四条 15%/20% 新轨迹均正常完成，体积、网格、夹持、功耗、链接、恢复和全部幅度响应门通过。仅两门失败：

- `CENTER_EPS150_SAVED_RESIDUAL = 0.0022842863 > 0.001`；
- `CENTER_EPS200_SAVED_RESIDUAL = 0.0017541406 > 0.001`。

两条轨迹各定相位保持的最后 1000 步中，残差仍持续下降。最慢关键状态的末值/前 1000 步值比例约为 `0.68–0.84`，没有出现体积安全停止、网格翻面或残差增长。因此失败归类为固定相位松弛不足，不是高幅度物理失稳。

## 唯一允许的修复

1. 只重算 `CENTER_EPS150` 与 `CENTER_EPS200`；
2. 每相位定值松弛由 3000 增至 6000 步；
3. 保持 `dt=0.02`、预平衡 1000 步、每段加载 50 步、8 个相位区段、材料、主动幅度、形态支撑、黏附、夹持和全部门限不变；
4. v01 两条 CENTER 失败轨迹、失败 verdict 和全部图片原样保留；
5. `SYNC_EPS150/200` 与既有 10% CENTER/SYNC 只在逐文件哈希匹配时引用，不复制、不重算；
6. v02 从组合证据重新生成逐细胞指标、真实形变图、5 倍显示图、牵引图和 9 状态动画。

## 裁决

只有两条修复轨迹通过原 `1e-3` 残差门，且组合的 61 门全部通过，才可裁决为 `passed_synthetic_isometric_high_amplitude_response`。不得放宽阈值、修改被动刚度或把等长幅度扫描解释为细胞材料柔软度标定；自由端/柔性端对照仍是独立的下一判别实验。
