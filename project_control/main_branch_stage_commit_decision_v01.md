---
decision_id: PRL-MAIN-BRANCH-STAGE-COMMIT-V01
decided_at: 2026-09-17
decider: user
status: approved
local_branch_policy: passed
remote_branch_merge: blocked
---

# 主分支阶段提交规则与远端核查

用户决定：“合并远端所有分支，以后阶段性进展直接提交到主分支，不用开新分支”。

## 当前事实与实施范围

本轮在 `E:\Temp-Projects\PRL` 核查：远端配置0、远端跟踪分支0、worktree 1；
唯一原本地分支为 `codex/clean-baseline`，HEAD为
`a61cb860cd83e50be59563f812390d3b9f7e2ab2`。这与此前经批准的无远端两提交基线一致，
不是网络失败，也没有发现可合并的远端分支。

已将现有本地分支直接改名为 `main`；不新建并行分支、不复制worktree、不改变原有两提交。
开始时暂存区为空，工作区有191个已修改或未跟踪文件；本次保留这些既有成果，
不将其整体混入规则提交。仅提交本决定与 `AGENTS.md` 新增的Git规则段落。
当前状态索引中的分支字段与驾驶舱Git信息同步到main；其既有科学内容不改写、不打包提交。

## 后续执行约定

1. 后续阶段默认在 `main` 实施。保存交付物并完成相应检查后，直接本地提交阶段进展。
2. 明确暂存范围，保留无关或来源未审阅的工作区改动；大型原始结果继续按存储合同保留在Git之外。
3. 失败阶段也可以提交代码修复与失败保全记录，但必须保留真实裁决，不宣称验证已通过。
4. 科学运行、清理、GPU和外部文件写入权限不因Git工作方式改变而扩大；FEM唯一主线不变。
5. 不自动推送、不强推、不删除分支、不改写历史、不执行破坏性工作区重置。

本决定只取代此前“固定使用codex/clean-baseline”及默认另开分支的工作方式。
旧清理记录保持历史原文；已退役Route H的分支/推送许可不自动适用于当前FEM主线。

## 远端合并的未完成项

状态为 `blocked`：当前没有远端地址可获取分支，无法声称“远端所有分支已合并”。
需要用户提供或指定准确远端仓库地址；收到后先只读核查其主分支与全部分支，
评估与本地精简基线的共同祖先、冲突和退役代码风险，再执行本次请求范围内的合并。
不自动使用 `--allow-unrelated-histories` 将已清理的旧历史重新引入。

本次没有访问远端，没有fetch/push，没有执行科学计算，没有删除文件或分支。

## 后续状态（2026-09-17）

上述“无远端”是本决定首次执行时的历史状态。用户随后提供并确认同项目仓库地址，
要求继续衔接；当前已完成六个旧分支历史到本地main的合并，FEM文件树保持不变。
见[衔接决定](remote_history_integration_decision_v01.md)及[执行验收](remote_history_integration_execution_v01.md)。
后续直接提交main的长期规则保持有效；普通推送状态以新执行记录为准。
