---
document_id: PRL-MIXED-CUBE-LOAD-DIAGNOSIS-V01
date: 2026-09-19
status: passed
authority: user requested continuing the scientific mainline and accelerating progress
scope: saved-state postprocessing only; zero new equilibrium solves
---

# 一个因果问题：积分误差，还是压力载荷的离散表示缺陷？

承接v03唯一下一步。此次“继续项目”用于已有状态诊断与必要的分析代码，不据此重开已消耗的原生求解批次。
原生产求解器、材料、网格、门限及全部失败包不修改。单代理、单CPU、0 GPU；不启动Docker。

## 固定的最小诊断

读取v03的κ=100、n=2/4/8三个已有终态；不计算新的位移/压力平衡。

1. 以原生产积分、Gauss-Duffy每轴6点和8点复算同一保存态的载荷与自由力残差。区分载荷积分差和本构内力积分差。
2. 在同一网格、同一精确运动学F*下分开组装等容应力与材料压力的节点虚功向量。
3. 构造压力—虚位移耦合矩阵 `B*[q,v]=∫q cof(F*):Grad(v)`，在X=0零虚位移空间中，将精确压力载荷向量投影到离散P1压力可产生的力空间。
4. 余量z满足B*z≈0且精确压力载荷对z仍做功时，直接证明该采样积分/网格下存在“离散压力无法平衡的压力载荷分量”。这只是固定解析几何上的节点系数欧氏最小二乘诊断，不是新的FEM平衡、inf-sup检验、全局Helmholtz投影或心室根因的充分证明。
5. κ=100→1000仅解析重标定相同F*的压力载荷；标注为反事实载荷诊断，不补造未运行的高κ终态。

准入和停止：预计外部新增16MiB、64MiB保全与10GiB余量；代码仓3GiB门保持。
离线分析最多300秒、分块内存；源哈希漂移、非有限/负J、装配不一致或投影证书不满足即停止，保留失败。
描述指标不新加科学否决门，不事后改写原精度failed。

## 交付与后续权限

create-only：`E:\Temp-Projects\PRL-results\ventricle_fem\mixed_cube_load_diagnosis_v01_20260919`。
使用共享只读原包与哈希，不复制大型数组。一个JSON表、必要派生数组、一个可重绘160dpi结构/结果诊断页。
沿用已采纳探索图例外，不制作论文终稿或重复Notebook图片包；图件数据链、PNG/SVG与目检仍保留。
源码、报告和小型预览检查后普通推送origin/main。完整原始结果继续外置，无删除/安装/全局记忆更新。
如证据要求新的对照求解，提出一次有界批次，另获用户确认；不跳到原心室主动、生长或FSI。

## 执行结果：压力载荷表示缺陷已定位，因果验证待控制组

离线80.126秒；3个已有终态，0次新平衡、0 Docker、0 GPU。639个源/保护文件哈希一致。
代码仓准入约1.90GiB，外部磁盘余量检查passed；42项针对性宿主测试passed。

| n | 无法表示的压力力分量/精确压力力范数 | 该分量/精确等容力范数 | 生产载荷积分差/高阶载荷范数 |
|---|---:|---:|---:|
| 2 | 29.1269% | 13.0467 | 0.0237615% |
| 4 | 12.0381% | 5.5010 | 0.000541900% |
| 8 | 3.27169% | 1.48822 | 0.0000101581% |

以上均是固定网格、自由节点系数上的欧氏范数，不是压力点值百分比、位移误差下界或可跨任意基底比较的连续范数。
令f_p为精确压力贡献，c最小化||f_p−B*ᵀc||²，z=f_p−B*ᵀc：
三网格均满足B*z≈0，且f_p·z=||z||²>0；缩放正规方程残差不超过8.62e−17。
因此确有当前P1压力无法通过虚功平衡的分量；κ=100最细网格中，它仍约为等容力的1.49倍。
解析F*不随κ改变，所以该压力力分量随κ线性缩放；这只是载荷的代数诊断，不是高κ新解。

最细网格原生积分与高阶载荷的差约3.44e−9，压力力余量约1.09e−3，相差约32万倍。
高阶6/8点规则的载荷差约5.73e−15；原解析体力/牵引与解析应力虚功不平衡≤9.47e−13。
这使“主要由载荷积分误差造成”的解释缺乏支持；也不能仅凭两个力范数就宣称已证明全部位移误差的单一原因。
原P2/P1、MMS载荷随κ增大的组合是现在最直接的候选机制；没有证据据此宣布锁死/inf-sup失稳。

压力误差污染速度的相关机制可参考[Linke等的Stokes研究](https://arxiv.org/abs/1609.03701)。
该文仅提供方法学类比，**不是**本项目有限变形固体的定理或原心室根因证明。

首次图件导出遇到可见域外log刻度布局检查失败；保留原绘图源码与失败记录，
仅改显式刻度位置后探索布局和目检passed，不改数值、不重新计算。600dpi投稿检查的两项失败照录，
因为此处按已采纳160dpi探索例外交付。没有建立论文终稿/多份Notebook图包。

下一步只保留[四工况控制批次](ventricle_mixed_cube_control_batch_v01.md)，用户随后明确整批确认；
当前not_run，接下来仅执行该有界控制。原八工况资格和原心室1%门均failed，三维主动/生长/FSI仍未运行。

封存：29文件、1104018 bytes；manifest SHA-256为
`fae715c181ee24be5ca384e6a43c0257fa52ffcad1d34c5a6a0a58e85652fac1`。
在线预览为239195字节原样PNG；完整结果仍在外部库，不把摘要当成完整原包。
