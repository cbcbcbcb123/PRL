# 本次精确授权

用户在本任务中对完整范围回复：**“批准该精确范围”**。

范围绑定 `frozen_manifest.json`，SHA-256：
`9f281fabde61c7fa2f7a3d0fee90e92303f3297983fbbb58cce2806b68439cd7`。

授权一次正常退出，必要时仅结束清单中身份一致的6个Docker残留进程；
仅用 `System.IO.Directory.Move` 移动两个精确源到指定新隔离位置；
随后一次启动和只读验收。状态漂移或失败即停。
不删除、不升级、不改设置、不触碰Docker\wsl/镜像/数据卷，不运行容器/FEM。

这份授权记录使冻结清单中“等待确认”的状态在本次操作范围内生效；不改写清单字节。
