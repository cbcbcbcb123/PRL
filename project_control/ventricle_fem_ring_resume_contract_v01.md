---
document_id: PRL-FEM-RING-RESUME-CONTRACT-V01
status: passed
accepted_at: 2026-09-18
authority: user latest explicit 同意，继续 after F6-S1-S3
execution_status: not_run
---

# F6-S1-S4：仅恢复圆环首个受压平衡

用户同意上一阶段唯一下一步。只将已验证的 `mat_mumps_icntl_14=100`
接回原圆环 P2/DG2 求解器；从 F6-S1-R 保存且接受的零载态继续
`p/mu=0.02, Ta=0`。零载不重算，最多1次非线性平衡调用，失败即停，无自动重跑。

## 不变项

- 原 M0 三层圆环：64周向分段、径向1/1/5、896个直边三角形；相同二次位移与逐单元二次压力映射。
- 无量纲平面应变 Neo-Hookean，mu=1、kappa=1000；同一随动腔压势。
- 外边界自由；(1,0)固定两个位移，(-1,0)固定y位移，仅去除刚体自由度。
- 主动应力=0，无实验标定；三层标签尚无不同被动材料参数。
- Newton/线搜索、30次上限、残量容差、6阶积分、独立验证实现不变。
- 所有原门保持：max|J-1|<=1%，独立自由力残量<=2e-6，压力弱残量<=1e-8，
  正J、重算应力/形变/反力一致；环解析响应误差<=5%，与同网格CG1面积响应差<=5%，
  DG2点态体积约束误差<=1e-9。不得放宽失败阈值。

## 实现和证据

扩展既有 `fem-fenicsx-pressure --resume-first-ring`，不复制第二套力学内核。
重建后逐数组验证网格及混合映射；对保留零载态独立复核后原样作为初值。
保存每一步真实SNES迭代向量、残量、最终F/J/应力/力与KSP/PC/MUMPS状态。
迭代过程是算法进度，不是生理时间；若不足5个真实状态，不伪造补帧。
独立后处理不调用FEniCSx求解或额外机械平衡；原失败包保持不变。

## 运行与交付

唯一新结果目录：`E:\Temp-Projects\PRL-results\ventricle_fem\f6s1s4_ring_first_pressure_v01_20260918`。
保留结果及相关祖先清单哈希；目录create-only，入口重复调用拒绝。
沿用本地已固定FEniCSx镜像、单CPU、8 GiB内存、禁网、0 GPU，1200秒上限。
启动前按现行外部结果库磁盘余量门准入，预计新增<=256 MiB，保全空间64 MiB；
运行时磁盘余量不足停止，不能自动删除科研证据。
禁止安装/拉取/升级/Docker修复/删除，禁止新的实际轮廓、细网格、额外压力、主动收缩、3D、FSI、生长计算。
输出可复算结构及真实结果图、执行裁决和导航；阶段只在main本地精确提交，不推送。
计算通过与工程交付通过分别记账，任何失败均保全并停止继续科学计算。
