# Paper 2 M2A FEniCSx 二维预检与冻结记录 v01

- 日期：2026-09-03
- 角色：Executor
- 上位合同：`paper2_m2_active_myocardial_fem_identity_conversion_contract_v01.md`
- 授权：`paper2_m2a_execution_authorization_decision_v01.md` 与
  `paper2_m2a_runtime_recovery_and_routine_autonomy_decision_v01.md`
- 结论：`PASS`，允许进入 M2A 二维同构比较域与结构门实现。

## 1. 运行时证据

- 容器：`dolfinx/dolfinx:v0.11.0`，本地镜像摘要保持不变；
- DOLFINx：`0.11.0.post0`；PETSc：`3.25.1`；SciPy：`1.17.1`；
- WSL2 内核：`6.6.87.2-microsoft-standard-WSL2`；
- 单进程 CPU，容器禁网，未使用 GPU。

原始记录：
`results/paper2_m2/preflight_fenicsx_2d_v01_20260903/preflight.json`。

## 2. 冻结的嵌套离散

| 层级 | x 分段 | 每个有限厚度层的 y 分段 | 两层探针三角形 | 预计完整状态自由度 |
|---|---:|---:|---:|---:|
| S0 | 8 | 2 | 64 | 222 |
| S1 | 16 | 4 | 256 | 758 |
| S2 | 32 | 8 | 1024 | 2790 |

三个层级在 x 与厚度方向均按 2 倍细化；节点集严格嵌套。M2A 采用固定的
左下—右上三角剖分、匹配界面和 x 向仿射周期边界。宏观轴向应变为两层与心内膜
共享的周期胞自由度，因此既保持周期性，也可定义受限缩短。

时间路径冻结为 `T64 -> T128 -> T256`。不得因结果不理想静默改变网格或时间步。

## 3. 求解器与容差冻结

C0/C1 沿用项目同义字段，数值分别保持为既有配置；线性 M2A 使用固定
`scipy_superlu` 直接解，并分别以 C0/C1 的 `newton_fatol` 检查线性残差。
直接解产生相同解并不免除 C0/C1 差异门；仍须逐端点报告其差值和残差。

不可变配置唯一入口：`src/paper2_m2/config.py`。所有生产记录必须写入其配置摘要。

## 4. 探针结果与资源包络

- S2 代表算子：1122 个向量自由度、14912 个非零元；
- S2 C0：441 次探针迭代，归一化残差 `9.14e-8`；
- S2 C1：456 次探针迭代，归一化残差 `9.20e-9`；
- 最细探针单次求解约 `0.0264 s`；
- 完整矩阵：`9 工况 x 2 表示 x 3 空间 x 3 时间 x 2 容差 = 324` 端点；
- 保守估计：每最细端点 `1 s`、总计 `324 s`；
- 冻结上限：每端点 `30 s`、总计 `7200 s`、峰值内存 `16 GiB`。

预检估计低于上限。生产运行若实际越过任一上限，必须返回 Human Gate，不得缩网格、
放宽容差或改工况。

## 5. 证据边界

本记录只证明依赖、二维装配和资源包络可行；没有证明结构门、数值收敛或 DCM--FEM
身份等价。后续仍按 `ID-P0/P1 -> ID-A1 -> 留出工况` 顺序 fail closed。
