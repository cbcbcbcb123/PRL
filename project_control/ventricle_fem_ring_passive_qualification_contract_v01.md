---
document_id: PRL-FEM-RING-PASSIVE-QUALIFICATION-CONTRACT-V01
status: passed
accepted_at: 2026-09-18
authority: user 统一，继续 interpreted as approval of the immediately preceding maximum-eight-state proposal
execution_status: not_run
parent_execution: project_control/ventricle_fem_ring_resume_execution_v02.md
---

# F6-S1-S5：补完DG2圆环被动压力与两网格资格

只执行用户刚同意的被动固体资格，不运行原轮廓、主动收缩、三维、血流/FSI、生长或ECM反馈。
复用现有Ring、运行控制器和独立NumPy验证器，不复制另一套求解内核。

## 最多8个新平衡与初值链

原G1的压力序列为p/mu=[0,0.02,0.04,0.06,0.08]。
M0：64周向×径向1/1/5，896三角形；M1：128×2/2/10，3584三角形。

1. M0原零载与0.02态只读复核并复用。从上阶段已接受0.02态继续0.04/0.06/0.08，共3次。
2. 仅M0全部通过后建立M1；从零向量计算零载及0.02/0.04/0.06/0.08，共5次。
3. 每态必须独立门通过才能进入下一态，任何失败保全当前/最后有效态后停止，无自动重跑或切换方案。
4. M0网格及全部混合映射须与上阶段逐数组一致；两网格位移几何、单元/固定标记须与原CG1对照一致。
   两级位移网格之间不做解插值来替代求解。

## 不改变的物理与门限

P2位移/DG2压力、有限bulk kappa=1000、mu=1、三维平面应变NH；半径20/27、21/27、22/27、1。
内壁随动压力、外壁自由；(1,0)固定ux/uy，(-1,0)固定uy去除刚体运动；Ta=0，三层被动参数暂相同。
原SNES/线搜索、30步上限与MUMPS余量100不变。DG2不宣称为严格不可压极限稳定元对。

- 全部求积点J>0且max|J−1|<=1%；独立自由力<=2e-6、弱压力<=1e-8、求解器自由残量<=1e-9。
- F/J重建误差<=1e-10，应力/力重建<=2e-7，压力虚功、闭合力/矩与规范反力各<=2e-6。
- DG2点态约束max|J−1−p_m/kappa|<=1e-9；p_m不是给定腔压。
- 恢复原完整G1解析门：各正压M0面积响应相对误差<=2%，M1<=1%；M1径向位移L2相对误差<=1%。
  解析解是不可压参照，不是有限kappa的精确解。上阶段仅首点的5%诊断门不能代替这些原G1门。
- 同网格CG1响应差<=5%，只读复用原对照，不重新求解CG1。
- 峰值p/mu=0.08的两网格腔面积响应绝对差<=0.002、相对差<=5%；其余压力差同样报告。
  两级通过是原合同数值资格，不等于多级渐近收敛或应力热点收敛证明。
- 原参考几何、积分映射和切线方向检查保持。每个非零态回读MUMPS INFOG=0和余量100。
  零载若0次Newton即收敛，没有实际因子分解，则该态MUMPS回读为not_run，不能伪报因子通过。

## 结果与停止保全

唯一create-only路径：`E:\Temp-Projects\PRL-results\ventricle_fem\f6s1s5_ring_passive_v01_20260918`。
保存两网格、不变标签/映射、全部接受u/p/F/J/应力/残量及所有实际Newton向量、初值链、运行参数和失败现场。
至少给结构、形变/应力/J图、原解析/粗细响应图，以及明确标注的5个真实压力状态（若失败仅显示实有状态）。
压力延拓和Newton迭代均不是生理时间；无插值伪帧。Notebook从物理复制输入复算并验收PNG/SVG。

一次固定本地FEniCSx镜像调用，单CPU、8GiB、禁网、0GPU、总时限1200秒。
预计新增256MiB仅作预测；停止保全至少64MiB、磁盘空余10GiB门和仓库3GiB硬限保持，不新增固定结果限额。
不重做已通过接口夹具；复核其冻结证据并在每个实际受压态核查因子回读。
旧失败包/接受态及所有父证据按哈希保护，无关50项修改保留。
不删除、安装、升级、拉取或修复/重启Docker；不启动额外容器试算、不自动重跑。
阶段进展在main精确本地提交，原始结果不入Git、不推送。
