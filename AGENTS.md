# PRL 项目工作约定

本文件是 Codex 或其他自动化执行者进入本项目后的首要工作规则。它只定义读取顺序、证据边界和执行约束，不自动授予新的科学或运行权限。

## 强制阅读顺序

开始任何项目任务前，依次读取：

1. `START_HERE.md`；
2. `memory/project_cockpit/status.json` 或渲染后的 `memory/project_cockpit/index.html`，仅用于快速定位；
3. `plan/INDEX.md`；
4. `plan/active/` 中与任务相关的外部专家方案包；
5. `project_control/CURRENT_STATUS.md`；
6. 当前有效的 `project_control` 决定、合同、失败冻结或执行记录；
7. 任务涉及的 `docs/`、代码、数据和结果。

驾驶舱是状态投影，不是权威证据。驾驶舱与计划、结果、Git 或当前决定冲突时，以后者为准，并将驾驶舱标记为陈旧或冲突后再更新。

若入口文件之间出现冲突，优先级为：

1. 用户最新明确决定；
2. `project_control/` 中适用范围内的最新有效决定或冻结约束；
3. `plan/active/` 中已登记的外部专家指导；
4. `planning/` 中的内部研究或投稿方案；
5. 临时对话、工作笔记或推测。

不得仅凭文件名中的 `latest`、`final`、`pass` 或较大的版本号判断优先级，必须读取状态、适用范围、取代关系和证据等级。

## `plan/` 的专用边界

- `plan/` 仅用于外部专家指导方案、其原始附件、忠实转录、来源信息、版本状态和完整性哈希。
- 外部专家原始材料不得静默改写；收到修订版时建立新版本包。
- Codex 自拟计划、内部投稿路线、执行合同、运行日志和计算结果不得放入 `plan/`。
- 专家意见是受控输入，不自动等于用户批准、科学事实或通过验证。
- 执行前必须在 `project_control/` 中找到相应采纳决定和执行合同；没有时应先形成待用户裁决的采纳矩阵。
- 专家原件或身份缺失时必须标记 `unknown`，不得根据转述补造来源。

## 执行与证据状态

- 执行状态统一使用 `passed`、`failed`、`blocked`、`not_run`、`unknown`。
- 计划完成、文件存在、测试通过和科学结论成立是不同层级，不得互相替代。
- 保留所有既有失败包、冻结记录、原始数据和历史版本；新工作使用新版本或明确允许的 create-only 路径。
- 科学结论、投稿主张、外部发布、提交、推送和 PR 按当前用户授权及有效项目决定执行。

## Git 主分支与阶段提交

- 根据用户2026-09-17的最新决定，本项目的长期工作分支为 `main`。后续阶段直接在 `main` 完成并提交，不默认新建任务分支或分支型 worktree；只有用户再次明确要求时才另开分支。
- 阶段性进展完成必要检查、保存交付物与证据后，直接创建本地阶段提交，无需再次逐次请示。提交说明必须区分工程完成、科学通过、失败保全和未运行项，不能把提交成功当成科学通过。
- 提交前核对分支、未提交改动、相关测试和暂存文件清单；只暂存本阶段明确涉及的路径或修改片段。保留其他未提交改动，不使用无边界的 `git add .`，不强制纳入被忽略的原始数组、构建、缓存或大型结果包。
- 合并远端分支前先核验真实远端、主分支、分支差异与历史关系；无远端时如实报告并索取仓库地址，不猜测或自行重连历史远端。分支合并不等于获准恢复退役科学路线。
- 本地提交不自动授权推送、强推、重写历史、删除分支或丢弃工作区改动。远端发布和破坏性操作仍按明确授权执行。
- 当前决定及本次远端核查见 `project_control/main_branch_stage_commit_decision_v01.md`。
- 用户已核验本项目远端为 `https://github.com/cbcbcbcb123/PRL.git`；本次新旧历史衔接及保留FEM内容的方式见 `project_control/remote_history_integration_decision_v01.md`，不得据此恢复DCM前向路线。

## 项目导航仓维护规则

- `START_HERE.md` 与 `memory/project_cockpit/` 首页只回答：项目目标、当前模型及边界/载荷、已完成到哪里、核心结果、当前主要难点、唯一下一步。
- 首页保持专家可快速阅读；路线图、活动事项、阻断和结果卡只保留当前决策必需的少量项目。历史过程、旧路线和详细失败账本放入 `task_log.jsonl`、`project_control/` 与 `results/`，不得为精简首页而删除证据。
- 每个实际完成的计算阶段至少给出模型结构图和一张直接支持裁决的结果图；尚未运行的阶段不得用概念图冒充结果图。
- 形态、场量或动力学阶段应保存多个明确标注的求解器状态；若时间未标定，必须写明是算法延拓或数值松弛进度，不得称为生理时间。
- 状态发生变化时同步更新 `status.json`、向 `task_log.jsonl` 追加一条记录、重新渲染 `index.html` 并执行严格链接验证。驾驶舱仍是状态投影，不替代合同、原始结果或独立复核。

## 安全边界

- 删除任何文件或目录前，必须列出绝对路径、确认不含原始材料或唯一成果，并取得用户明确确认。
- 启动 GPU worker、安装或升级软件、改变权限、连接外部服务、创建持久服务，均须单独取得用户确认。
- 默认禁止在项目工作区外新建文件或文件夹。用户已长期授权将与本项目有关的公开科研大型数据包及其必要来源元数据，按 `E:\Data\<project>\public\<provider>\<stable_record_id>\` 分类下载和保存；该例外不覆盖其他外部路径、软件安装包、临时计算输出、删除/覆盖、虚拟盘、持久服务或 GPU 任务。每次获取仍须登记来源、许可、版本、字节数和哈希，先审计归档再按需解包，PRL 仓库只保留小型清单、预览和验证结果。
- 默认禁止创建或挂载虚拟盘符、目录映射或网络驱动器。
- 修改或生成答辩 PPT 图片时，依次使用内容结构、科研绘图提示词、Image Gen 三阶段流程。
- 项目内临时目录必须记录任务所有权和用途；不得清理任务开始前已经存在的临时目录。

## 目录职责

- `plan/`：外部专家指导；
- `memory/project_cockpit/`：项目目标、阶段、证据边界和下一步的可视化状态投影；
- `planning/`：内部研究、论文结构和投稿路线；
- `project_control/`：采纳决定、合同、门禁、执行记录和失败冻结；
- `docs/`：稳定理论、架构与 ADR；
- `02_图表/Figures/`：可复算的论文图版本包；
- `figures/`：长期示意图和历史图形资产；
- `artifacts/`：渲染、审阅和交付产物；
- `results/`：计算结果及正式摘要；
- `tmp/`：非权威临时产物。

详细职责与本轮审计证据见 `project_control/repository_directory_responsibility_audit_v01.md`。
