#pragma once

// X1-A makes the controlled cell-engine fork the single source of truth for
// this ABI. Keep the historical PRL include path as a compatibility facade.
#include <prl_cell_engine/remesh_contract.hpp>
